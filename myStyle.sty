% \usepackage[longnamesfirst]{natbib}
\usepackage[backend=biber,style=jpa]{biblatex}
\addbibresource{../../myBiber.bib}
\addbibresource{JPA92.bib}
\usepackage{bxpapersize}
% 1行49文字。200行で一万文字程度(目安)
\usepackage[top=25truemm,bottom=25truemm,includefoot,includehead,heightrounded]{geometry}
\usepackage{amsmath} %AMS系数式パッケージ
\usepackage{amssymb} %実数集合とかのフォント
\usepackage{url} %URLをきれいに表示
\usepackage{graphicx} %図版を読み込む
\usepackage{float} %図の強制配置[H]オプション用
\usepackage{lineno} %行番号表示パッケージ
\usepackage{lscape} %横向きの表
\usepackage{luatexja-ruby} %ルビを振る
\usepackage{epsdice} %サイコロの図がでるパッケージ
\usepackage{fancyvrb} % verbatim環境の拡張版
\usepackage{ulem} % 下線を引く
\usepackage{colortbl} % 表の色付け（bmより前に読み込む必要がある）
\usepackage{bm} %ベクトル表記が楽になるパッケージ
\usepackage{pdfpages} %PDFファイルを取り込むパッケージ。表紙の図版に。
\usepackage{wrapfig} % 図の周りに文章を配置するパッケージ
\usepackage{multirow} % 表の縦結合
\usepackage{tikz} % コマンドで図を書くパッケージ
\usetikzlibrary{positioning, arrows.meta,intersections, calc} % Tikzのパッケージ
\usetikzlibrary{shapes, arrows}
\usepackage{tikz-3dplot}
\usepackage{lltjext}% 部分的に縦書き
\usepackage{longtable} %長い表のページ跨ぎ
\usepackage{tablefootnote} %表中の脚注。tabular環境では脚注が無効化されるので。
\usepackage{luatexja-fontspec}
\setmainfont{TeX Gyre Termes} % 英語フォント設定（イタリック体のため）
\usepackage{nicematrix} % 階段行列に色付けしたり
% \usepackage[no-test-for-array]{nicematrix}
\usepackage{booktabs} %表の罫線パッケージ
\usepackage{accents} % \bar{\bar{X}}対策
% BIZ UDフォントを使う
\newjfontface{\nonproportional}{BIZ UDMincho Medium}[YokoFeatures={JFM=ujis}]
\setmainjfont[Ligatures={Common,TeX},
  ItalicFont={BIZ UDPMincho Medium},
  ItalicFeatures={FakeSlant=0.27},
  SlantedFont={BIZ UDPMincho Medium},
  SlantedFeatures={FakeSlant=0.18},
  BoldFont={BIZ UDPGothic Bold},
  BoldSlantedFont={BIZ UDPGothic Bold},
  BoldSlantedFeatures={FakeSlant=0.18},
  BoldItalicFont={BIZ UDPGothic Bold},
  BoldItalicFeatures={FakeSlant=0.27},
  YokoFeatures={JFM=prop}]{BIZ UDPMincho Medium}


% ソースコード用のパッケージと設定
\usepackage{xcolor}
\usepackage{listingsutf8}
\renewcommand{\lstlistingname}{code :}
\usepackage{hyperref} %ハイパーリンク   設定

% ソースコードのフォントを変えたい
\lstset{
    frame=single,
    backgroundcolor={\color[gray]{.95}},
    keywordstyle={\color{magenta}},
    stringstyle=\color{blue}, 
    basicstyle=\ttfamily\small,
    numberstyle={\ttfamily\small\color{black}},  
    commentstyle = {\ttfamily \color[cmyk]{1,0.4,1,0}},
    numbers=left,
    numbersep=10pt,
    tabsize=2,
    extendedchars=true,
    xleftmargin=17pt,
    framexleftmargin=17pt
}
\definecolor{pinegreen}{rgb}{0.0, 0.47, 0.44}
% Hyper Linkを目立たせない呪文
\hypersetup{
setpagesize=false,
 bookmarksnumbered=true,%
 bookmarksopen=true,%
 colorlinks=true,%
 linkcolor=blue,
 citecolor=pinegreen,
 urlcolor=magenta
}

% 強調表現/索引
\newcommand\keyterm[3]{\textbf{#1(#3)}\index{#2@#1}}
\newcommand\keytermL[2]{\textbf{#1}\index{#2@#1}}
\newcommand\keytermE[1]{\textbf{#1}\index{#1}}

% 枠囲いのパッケージ
\usepackage{tcolorbox}
\tcbuselibrary{breakable, skins, theorems}
% スクリーン出力
\newtcbtheorem[number within=chapter]{Rscreen}{Rの出力}{fonttitle=\gtfamily\sffamily\bfseries\upshape,colframe=gray,colback=blue!3!,fontupper=\gtfamily\small}{RS}
\newtcbtheorem{SCrstan}{rstanの出力}{fonttitle=\gtfamily\sffamily\bfseries\upshape,colframe=gray,colback=green!3!,fontupper=\gtfamily\small}{SCrstan}
\newtcbtheorem{SCcmd}{cmdstanrの出力}{fonttitle=\gtfamily\sffamily\bfseries\upshape,colframe=gray,colback=pink!3!,fontupper=\gtfamily\small}{SCcmd}
\newtcbtheorem{MCMCout}{MCMCの結果}{fonttitle=\gtfamily\sffamily\bfseries\upshape,colframe=gray,colback=cyan!3!,fontupper=\gtfamily\small}{MCMCout}

% % コラム
% breakableオプションが怪しいのでコラムそのものを廃止
% \newtcbtheorem{Untiku}{うんちく}{fonttitle=\gtfamily\sffamily\bfseries,colframe=blue,colback=blue!7!,fontupper=\gtfamily\small}{Untiku}
% 例題

% \newtcbtheorem[number within=chapter]{reidai}{例題}{
\newtcbtheorem{reidai}{例題}{
    fonttitle=\gtfamily\sffamily\bfseries\upshape,
    colframe=gray,
    colback=white,
    rightrule=0pt,
    leftrule=0pt,
    arc=7pt,
    bottomrule=1pt
    }
{reidai}

% hinto
% \newtcbtheorem[number within=chapter]{hinto}{ヒント}{
\newtcbtheorem{hinto}{ヒント}{
    fonttitle=\gtfamily\sffamily\bfseries\upshape,
    colframe=cyan,colback=gray!10,rightrule=0pt,leftrule=0pt,arc=7pt,bottomrule=1pt
}{hinto}

% figure環境の外でのcaption
\usepackage{capt-of}


% FAQ box
\newtcbtheorem[number within=section]{FAQ}{Q.}
{
    fonttitle=\gtfamily\sffamily,
    skin=bicolor,
    colframe=gray!2,
    colback=gray!10!white,
    rightrule=10pt,
    leftrule=10pt,
    bottomrule=2pt,
    colbacktitle=red!65,
    theorem style=standard,
    arc=3pt,
    colbacklower=green!5
}{FAQbox}


% 索引
\usepackage{makeidx}
\makeindex

% 索引スタイル
\newcommand{\groupedheadprefix}[1]{%
    \par%
    \vspace{\baselineskip}%
    \hfil{\normalsize{\bfseries #1行}}\hfil%
    \vspace{\baselineskip}\par}

% ヘッダーにバージョン情報を
\usepackage{fancyhdr}
\pagestyle{fancy}
    \cfoot{\thepage} %フッタ中央．ページ番号を表示
    \renewcommand{\chaptermark}[1]{\markboth{第\ \thechapter\ 講 \ #1}{}}

% bookクラス用の参考文献名の設定
\renewcommand{\bibname}{引用文献}
% 脚注でもverbatim環境が使いたい
\VerbatimFootnotes

% ギリシア数字のIIとかIIIとか
\newcommand{\2}{\mathrm{I}\hspace{-1.2pt}\mathrm{I}}
\newcommand{\3}{\mathrm{I}\hspace{-1.2pt}\mathrm{I}\hspace{-1.2pt}\mathrm{I}}
\newcommand{\4}{\mathrm{I}\hspace{-1.2pt}\mathrm{V}}
% マル1,マル2,マル3,マル4
\newcommand*\circled[1]{\tikz[baseline=(char.base)]{
            \node[shape=circle,draw,inner sep=2pt] (char) {#1};}}


%% fancyhdr Warning対策
\setlength{\footskip}{16pt}

%% すべての数式はディスプレイスタイルにするのだ
\everymath{\displaystyle}