\documentclass{ltjsbook}
\usepackage{docmute} 
\usepackage{../../myStyle} 

\begin{document}
% \chapter{確率的プログラミング；状態空間モデル}
\chapter{Probabilistic Programming; State Space Model}
\label{x29_SSpace}
% 前回は体重の変化データを例に，データの平均点が変わるモデル，変化点を検出したり変化点を境に傾きが変わる線形モデルをみてきました。
Last time, using the example of weight change data, we looked at models in which the average of the data changes, and linear models where the slope changes across a point of change.

% 同じように，今回も時系列的なデータを扱います。前回の最後に案内したように，時系列データに普通の回帰直線をそのまま当てはめることは適切ではありません。どのような注意点が必要なのかについて，少し考えてみましょう。
Similarly, we will deal with time series data this time as well. As advised at the end of last time, it is not appropriate to directly apply a normal regression line to time series data. Let's consider what precautions are necessary.

% \section{時系列データの特徴}
\section{Features of Time Series Data}
% 時系列的なデータはどのようなときに得られるでしょうか。前回導入した時のように，誰か1人が日々の生活を記録し続けている，それも立派な時系列データです。
When would you be able to obtain time-series data? Just like introduced last time, it is also a great example of time-series data when one person continuously records daily life.
% 心理学には\keytermL{日誌法}{にっしほう}と呼ばれるデータ収集法があります。一言で行ってしまえば日記なのですが，日々多くの出来事や感情が到来するのを後から振り返って考えるのでは，正しく思い出せなかったり記憶が歪んでしまうこともあります。日々の状態を細かく記録し積み重ねることは，数字になっていなくても貴重なデータなのです。また最近は，\keytermL{経験サンプリング}{けいけんさんぷりんぐ}と呼ばれる調査法もあります。これは決まった間隔で質問紙がスマートフォンなどに送られてきて，定期的に心情を心理尺度に反映させていく，というものです。こうしたことができるようになったのには，私たちの周りに電子的なデバイスがたくさんあることが理由の1つです。さらに最近ではウェアラブル端末といって，身につける電子端末がありますね。Apple Watchなど携帯電話と同じような機能を持ったこれらのデバイスは，GPS機能がついていたり，万歩計による歩数の計測，血圧・脈拍のリアルタイムな計測が行われていたりします\footnote{こうしたデータは\keyterm{ライフログ}{らいふろぐ}{Life-log}と呼ばれることもあります。}。これを蓄積したデータとすると，かなりの時系列的なデータが手に入ることになります。
In psychology, there is a data collection method called the "diary method". In a word, it is a diary, but if you look back at the many events and emotions that arrive every day, you may not remember correctly or your memories may be distorted. Detailed recording and accumulation of daily conditions is valuable data even if it is not in numerical form. Recently, there is also a survey method called "experience sampling". This is where a questionnaire is sent to your smartphone at regular intervals, and you regularly reflect your feelings on psychological scales. One reason why we can do this is that we have many electronic devices around us. Recently, there are electronic devices that you can wear, such as Apple Watch. These devices, which have the same functions as mobile phones, have a GPS function, measure the number of steps by a pedometer, and measure blood pressure and pulse in real time. This accumulated data can provide a substantial amount of chronological data.

% あるいはまた，社会的なデータも時系列的に得られるものが多いです。一番わかりやすいのは株価の指標などでしょうか。日経平均株価など，時事刻々と変化する株価がグラフになって表されているのをみなさんもみたことがあると思います。また，TwitterなどのSNSで，今どのようなキーワードが使われているかといったことが，リアルタイムに計測されます。これらの指標は個々人の特徴というより，社会全体のうねりのようなものを表現していることになりますが，これを研究することも立派な社会心理学的テーマになり得ます。時系列データは，人文社会科学領域ではこれまで経済学やマーケティングなどが専門的に扱ってきたところがありますが，これからは心理学の領域でもこうしたデータを分析することが流行してくるかもしれません\footnote{もちろん生理心理学や動物心理学では，古くからこうしたデータを扱ってきています。}。
There are also many types of societal data that can be obtained over time. Perhaps the most straightforward examples would be stock market indicators. I believe you have all seen time-sensitive stock prices such as the Nikkei Stock Average represented graphically. Additionally, on social media platforms like Twitter, the current usage of specific keywords can be measured in real time. Although these indicators express more of a societal fluctuation rather than a specific individual's characteristics, research on them can certainly become a respectable topic in social psychology. Time-series data has been a specialized field in humanities and social science, dealt mainly in economics and marketing, but going forward it's likely to become common to analyze this kind of data within the field of psychology as well. Of course, physiological psychology and animal psychology have been dealing with this kind of data for a long time.

% さて時系列的なデータはどういった特徴があるかと言うと，端的に言えば\keyterm{自己相関}{じこそうかん}{auto correlation}がある，ということでしょう。すなわち，ある時点のデータはその前の時点と相関している，ということです。体重のデータの場合でもそうですが，明日の体重は突然ランダムな値になるのではなく，今日の体重に応じて変化するはずだからです。また，データの変動に周期的なパターンが存在することもあります。脳波などのデータは基本的に波ですから，大きな波，小さな波がパターンを描いています。株価など社会的なデータも，季節による一定の変動など全体的な揺らぎがある上で，目的とする意味のある変化を見つけ出さなければならないという課題に取り組んでいます。たとえば周波数のデータの平均はゼロになりますから，基本的な記述統計では太刀打ちできないところがあります。回帰分析もその前提として，各データが独立に得られていると言うものがありますから，自己相関するデータは当然この仮定に違反していることになります。こうしたデータを分析するためには，周波数の大きさをスペクトル解析するとか，多次元の行列であるテンソルを使ってさまざまな要素を分解する，といった特殊な応用数学を駆使する必要があります。
So, what are the characteristics of time-series data? To put it simply, it has something called \keyterm{auto correlation}. In other words, the data at a certain point in time is correlated with the data from the previous point in time. Even for weight data, the weight for tomorrow is not going to suddenly become a random value, it is going to change based on the weight today. Also, there may be a periodic pattern in the variation of the data. Data like brain waves essentially describe waves, so they draw patterns with big and small waves. Social data like stock prices also have overall fluctuations, such as certain changes based on the season, and there's the challenge of finding meaningful changes in this context. For example, the average of frequency data becomes zero, so there are parts that traditional descriptive statistics can't handle. Regression analysis also assumes that each piece of data is obtained independently, so data with auto correlation naturally violates this assumption. To analyze such data, you need to apply specialized mathematics, such as spectrum analysis of the size of the frequency spectrum or decomposing various elements using a tensor, which is a multi-dimensional matrix.


% 今回はこうしたさまざまなモデルの中でも，\keyterm{状態空間モデル}{じょうたいくうかんもでる}{State Space Model}を紹介します。
This time, we will introduce the \keyterm{State Space Model}{じょうたいくうかんもでる}{State Space Model} among various models.
% \section{状態空間モデル}
\section{State Space Model}
% 時系列的なデータは，自己相関すなわち前の時点の状態が，次の時点に影響しているという特徴があり，これをうまく表現するのが状態空間モデルです。状態空間モデルはこれまでの\keytermL{モデリング}{もでりんぐ}の技法を使うと簡単に実装できます。というか，これまでの時系列的なデータ解析は，自己相関を減らすために一時点前のデータとの差分をとってそれをモデルにする，時点ごとの変化をスムージングするなど，さまざまな工夫の上に成り立っていました。状態空間モデルはそれを非常にすっきり表現してくれているので，それまでの分析ノウハウがなくとも誰でも簡単に利用できるモデルだといえるかもしれません。
Time-series data has a feature that self-correlation, or the state of a previous point in time, affects the next point. This is well expressed by the state space model. The state space model can be easily implemented using the modeling techniques we have used so far. In fact, previous time-series data analysis was based on various ingenuities, such as taking the difference with the data from one time point before to reduce self-correlation and modeling it, or smoothing the changes at each point in time. The state space model expresses this very neatly, so it could be said that it is a model that anyone can use easily even without previous analysis know-how.

% 状態空間モデルは，まず観測されたデータと，その背後にある\textbf{状態}を区別します。
The state space model first distinguishes between the observed data and the underlying \textbf{state}.
% 体重変化のデータの例で考えてみましょう。計測された体重は，当然体の重さを反映したものですが，測定の時に多少の誤差が生じるでしょう。例に使っている筆者のデータでも，毎朝全裸で計測しているわけではありませんから，季節の変わり目で薄いパジャマからジャージにしたり，冬用の厚手のパジャマに変えたりすると，それだけで計測された数字には違いができます。本来知りたいのは，体の重さ，肉の重さそのものなはずですね。ここでいう体重そのもの，計測誤差のない肉の重さが，ここでいう状態のことになります。
Let's consider the example of weight change data. The measured weight, of course, reflects the weight of the body, but there may be some errors at the time of measurement. Even in the author's data used for the example, I am not measuring completely naked every morning, so if I change from light pajamas to sweatpants at the change of seasons, or if I change to thick winter pajamas, it makes a difference in the measured numbers. What I really want to know is the weight of the body, the weight of the meat itself. The weight of the body itself, the meat without measurement errors mentioned here, becomes the state mentioned here.

% そして$t$時点の状態$\mu_t$は，次の時点の状態に影響します。服の重さなどを取り除いた$t$時点の肉の状態に，食事や運動といった変化が加わって明日$t+1$時点の肉の状態になるわけですから，$\mu_{t+1} = f(\mu_t)$と考えることができるわけです。
And the state at time $t$, $\mu_t$, influences the state at the next point in time. Removing things like the weight of clothes, the state of the flesh at time $t$, with changes such as meals and exercise, becomes the state of the flesh at time $t+1$ tomorrow, so we can think of it as $\mu_{t+1} = f(\mu_t)$.

% もし翌日の体重が，今日の体重にちょっとした誤差がつくような変動しかしない，と言うのであれば$\mu_{t+1} \sim N(\mu_t,\tau)$と表すことができるでしょう。あるいは，運動量$P_t$の関数だというのであれば，$\mu_{t+1} \sim N(\mu_t + \beta_1P_t,\tau)$のように回帰分析の確率モデルのように表現すれば良いでしょう。もちろん説明変数が増えたり，一次関数ではないものを考えても構いません。
If you say that the body weight of the next day only fluctuates with a slight error in today's body weight, it can be expressed as $\mu_{t+1} \sim N(\mu_t,\tau)$. Or, if it is a function of the momentum $P_t$, it could be expressed as a probability model of regression analysis like $\mu_{t+1} \sim N(\mu_t + \beta_1P_t,\tau)$. Of course, it is okay to consider something other than a linear function, even if the explanatory variables increase.

% 一方，計測値$W_t$は，この$\mu_t$に誤差$\sigma$がついて得られる，すなわち$W_t \sim N(\mu_t , \sigma)$という関係になります。これらの関係を表現したのが，図\ref{fig:29_01}になります。
On the other hand, the measured value $W_t$ is obtained by adding an error $\sigma$ to this $\mu_t$, in other words, it becomes a relation of $W_t \sim N(\mu_t, \sigma)$. The figure\ref{fig:29_01} represents these relationships.
\input{x29_SSpace_escape_01}
% このようにして，状態が時間$t,t+1,t+2,\cdots$を通じてつながっており，状態に応じて観測値が得られると考えます。観測値を手に入れる際には偶然誤差$\sigma$が生じますし，状態の変化は確率的に変わるのであれば幅$\tau$で，系統的に変わるのであればそれをモデリングしてやれば良いことになります。
In this way, states are connected over time $t, t+1, t+2, \cdots$, and observation values are obtained depending on the state. When obtaining observation values, a random error $\sigma$ occurs, and if the state changes probabilistically, it can be dealt with a width of $\tau$, and if it changes systematically, it should be modeled.

% この発想に基づいて，設計図を書いてみましょう。ここでは体重の変化に特別な傾向を考えず，ただ幅$\tau$で確率的にノイズが加わる\keyterm{ホワイトノイズモデル}{ほわいとのいずもでる}{white-noise model}で考えてみることにします。
Based on this idea, let's try to draw a design diagram. Here, we consider a white noise model, where random noise is added with a width of $\tau$, without considering any special trends in weight changes.
% 少しイメージしにくいかもしれませんが，たとえば次のように表現できます(図\ref{fig:29_02})。
It may be a little hard to imagine, but for example, it can be expressed as follows (Figure \ref{fig:29_02}).
\input{x29_SSpace_escape_02}

% それではこれをコードにしてみましょう。少し長くなりましたが，ホワイトノイズのモデルはコード\ref{stan::29_01}のようになります。
Let's try to code this then. It got a little long, but the white noise model looks like code \ref{stan::29_01}.
\input{x29_SSpace_escape_03}
% \paragraph{コード解説}
\paragraph{Code explanation}
\input{x29_SSpace_escape_04}

% これを使って，体重のデータを分析してみましょう。データは2020年からのものを使います。図\ref{fig:29_03}をみると，2021年まではあまり変化がなく，21年初頭に少し体重が下がって，また上がって，その後5月から謎の下降\footnote{自転車事故による鎖骨骨折と，それに伴ってお箸持ちにくくて食事量が減る，運動も減るといったのが原因でしょう。}，そしてその反動\footnote{悔しいです。}，とおそらく線形モデルでは表現できないような複雑な動きをしています。
Let's use this to analyze the weight data. The data used is from 2020. Looking at Figure \ref{fig:29_03}, there isn't much change until 2021, when the weight drops a little at the beginning of the year, then rises again, and then there's a mysterious drop from May\footnote{This is probably due to a bicycle accident resulting in a broken collarbone, which made it difficult to hold chopsticks and reduced the amount of food and exercise.}, and then a backlash\footnote{It's frustrating.}. It's probably a complex movement that can't be represented by a linear model.

\input{x29_SSpace_escape_05}


% さて，コード\ref{code:29_01}を実行し，図\ref{fig:29_04}のような結果を得ます。複雑な動きについても，モデルがしっかりと予測しているのがみて取れますね。状態空間のパワフルな表現力をお楽しみいただけましたでしょうか。
Well, when you run the code \ref{code:29_01}, you get a result like figure \ref{fig:29_04}. You can see that the model predicts well, even for complex movements. Did you enjoy the powerful expressiveness of state space?
\input{x29_SSpace_escape_06}
% \paragraph{コード解説}
\paragraph{Code Explanation}
\input{x29_SSpace_escape_07}

\input{x29_SSpace_escape_08}


% \section{欠損値の補間}
\section{Interpolation of Missing Values}
% ところで，データをよくみてみると，実は毎日のデータになっていないところがあることに気づきます。いや，気づかないかもしれないですね。次のコード\ref{code:29_02}を実行してみてください。
By the way, if you look closely at the data, you'll notice that some parts aren't actually daily data. Well, you might not notice it. Please try running the following code \ref{code:29_02}.
\input{x29_SSpace_escape_09}
% \paragraph{コード解説}
\paragraph{Code Explanation}
\input{x29_SSpace_escape_10}
% これをみると，ちょくちょく抜けていて，時には4日も空いていたりすることがわかります\footnote{2020年2月22日から24日は岡山県にベイズ塾合宿(出張)でした。コロナがまだ豪華客船の中だけに抑え込まれている時期で，ギリギリ出張できた時期です。このあと大学の卒業式がなくなったり，前期オンライン授業になったりと言う，波乱の2020年度が始まったのでした。}。ともかく，これでは正確にデータを分析できているとは言えません。
Looking at this, you'll see that there are often gaps, sometimes even as long as four days \footnote{From February 22nd to 24th, 2020, we had a Bayes Training Camp (business trip) in Okayama Prefecture. This was a time when the Corona was still only contained within the luxury cruise ship, and we were barely able to go on the business trip. Afterwards, university graduation ceremonies were cancelled, and the first semester moved to online classes, marking the start of a turbulent 2020 academic year.}. Anyway, with this, it can't be said that the data is being accurately analyzed.
\input{x29_SSpace_escape_11}
% しかし計測はできていなくても，出張中にも状態の変化は続きいているはずで，次の計測時点はその日の状態の関数になっているはずです(図\ref{fig:29_05})。
Even if measurements are not being made, changes in the state should continue during the business trip, and the next measurement point should be a function of the state of that day (Figure \ref{fig:29_05}).
\input{x29_SSpace_escape_12}

% この欠損した状態のデータをどのように考えれば良いでしょうか。実はこのことのヒントはすでに，最初のコードの中に入っています。
How should we consider this missing state of data? In fact, the hint for this has already been included in the initial code.
% 状態空間モデルの1時点目，$\mu_1$は$\mu_0$から影響されているはずですが，データから$\mu_0$はわかりません。しかしわからないことは確率で表現するのがベイジアンのやり方です。
The first point of the state space model, $\mu_1$, should be influenced by $\mu_0$, but we don't know $\mu_0$ from the data. However, the Bayesian way is to express what we don't know in terms of probability.
% 今回も，欠損している測定値はわからないものとして確率で表現し，推定してやれば良いのです！このようにして間を埋めることをとくに\keyterm{補間}{ほかん}{interpolating}と言います。
This time too, the missing measurements should be expressed in probabilities and estimated! This specific way of filling in the gaps is called "interpolation".

% 補間をするためには，Rコードの方でもStanコードの方でもすこし工夫が必要です。
In order to interpolate, some ingenuity is required in both R code and Stan code.
% まずRのほうで，データを加工するところから見てみましょう。Stanには欠損値\verb|NA|を与えることはできませんから，欠損しているところには特別なあり得ない数字，そうですね，999という数字でも入れておきましょう。
Let's start by looking at how to process data in R. You can't give missing value "NA" to Stan, so let's put a special impossible number, say, 999, where there are missing data.


\input{x29_SSpace_escape_13}
% \paragraph{コード解説}
\paragraph{Code Explanation}
\input{x29_SSpace_escape_14}

% このようにすることで，次のようなデータセットができました(出力\ref{RS:out29_02})。
By doing so, we were able to create a dataset as follows (Output \ref{RS: out29_02}).
\input{x29_SSpace_escape_15}
% これをみると，2020年1月12日は欠損だったのですが，データ上は999という数字が入って完全データのように見えます\footnote{ちなみに2020/01/11から12にかけては，犬会という研究会があったため関西に出張していました。宿に泊まったので，12日の朝のルーティンができなかったのです。}。このデータをStanに与えてやり，体重が999というあり得ない数字の場合は別の処理をする，という0過剰ポアソンの技術を応用します(セクション\ref{ZIpoisson}，Pp.\pageref{ZIpoisson})。
Looking at this, the data for January 12, 2020 was missing, but the number 999 is entered in the data, making it look like complete data\footnote{Incidentally, I was on a business trip to Kansai for a study group called "Dog-kai" from January 11 to 12. I stayed at a hotel, so I couldn't do my morning routine on the 12th.}. Applying the technique of zero-inflation Poisson that processes the data differently when the weight is an impossible number like 999, this data is fed to Stan (Section \ref{ZIpoisson}, Pp. \pageref{ZIpoisson}).
\input{x29_SSpace_escape_16}

\input{x29_SSpace_escape_17}

% このようにして，推定してみましょう。Stanに与えるデータセットは次のようにして作ります。
Let's try estimating like this. The dataset given to Stan is created as follows.
\input{x29_SSpace_escape_18}
% 欠損値の数は，オブジェクト\verb|fullDays|の\verb|weight|変数が999かどうかを判定させ(\verb|==|でイコールかどうかという論理判断になります)，条件が合致した数を数え上げるという方法をとっています。
The number of missing values is determined by checking if the 'weight' variable of the object 'fullDays' is 999 (with '==' making a logical decision of whether it is equal), and then counting the number of matches.

% コードが走り出すと，推定は順調に進むと思います。が，結果の出方が少しややこしいです。状態$\mu_i$はすべての日程について推定されますが，これに加えて欠損があったデータも推定されていきますので，描画する際は「実測値なのか推定値なのか」，「補間した欠損の推定値は何番目の欠損値だったか」を判断しながら結合していき，データにする必要があるからです。Stanの中で作ったカウント変数$j$をRの方でも考えてやらないといけないわけですね。
Once the code starts running, I think the estimation will proceed smoothly. However, the way the results come out is a bit complicated. The state $\mu_i$ is estimated for all schedules, but in addition to that, the missing data is also estimated, so when drawing, you need to determine whether it is "an actual value or an estimated value" and "what was the missing value estimated by the interpolation?", and you need to combine them into data. In other words, you need to think about the count variable $j$ that we made in Stan on the R side as well.
\input{x29_SSpace_escape_19}
% \paragraph{プログラム解説}
\paragraph{Program Explanation}
\input{x29_SSpace_escape_20}
%    \item[関数の結果を受けとる] \verb|plot.tmp|は描画用の一時データオブジェクトです。ここに全期間のデータと，今回の状態推定値，欠損推定値を入れておきます。
   \item [Receiving the results of the function] \verb|plot.tmp| is a temporary data object for drawing. Store all period data, current state estimation values, and missing estimation values in this object.
%    \item[描画する] x軸は全日程です。まずは体重$W_j$を\verb|geom_point|と\verb|geom_errorbar|でプロットします。欠損の場合は推定値が入っています。次のポイントとエラーバーのgeomは，状態$\mu$のものになります。
\item[Plotting] The x-axis corresponds to the entire schedule. First, the weight $W_j$ is plotted using \verb|geom_point| and \verb|geom_errorbar|. If missing, an estimated value will be used. The next geom of points and error bars will be those of the state $\mu$.

\end{description}

% さあどうでしょう。コードはともかく，図\ref{fig:29_05}を見てみましょう。実際に観測しているのは黒点，状態の推定値は緑点と赤いエラーバーで表示されています。青いバーは欠損した時の推定値で，値がないのですから当然触れ幅は大きいですが，おそらくこの体重が測定できなかった日はこれぐらいだったんじゃないか，という値が推定されているわけです。数字で見ると，たとえば2020年1月12日のMAP推定値は83.4kg，95\%区間でいうなら[82.6,84.2]です。前日が84.3kgで翌日が83.1kgですから，そんなもんかなあという気もします。
Well, let's see. Regardless of the code, let's take a look at figure \ref{fig:29_05}. What we are actually observing is the black dot, the estimated value of the state is displayed in green dots and red error bars. The blue bar is the estimated value at the time of missing, the value is not there so of course the width is large, but probably the value that it was about this on the day when this weight could not be measured is estimated. If you look at the numbers, for example, the MAP estimate for January 12, 2020 is 83.4kg, and if you say it in the 95% interval, it is [82.6,84.2]. The day before was 84.3kg and the next day was 83.1kg, so it seems like that.
\input{x29_SSpace_escape_21}

% ところで，測定していないはずの日も補間できるよ，ということを考えてみると，これはすごいことだと思いませんか。そうです，未来の値ももちろん「測定していない日」ですから，このモデルを使うと未来予測ができるのです！すごい！
By the way, considering that we can also interpolate the days we should not have measured, don't you think this is amazing? Yes, the future values are of course the "days we haven't measured", so we can predict the future using this model! Amazing!

% 早速同じコードで，日程を未来に飛ばしてみましょう。データは2021年12月06日までしかありません\footnote{この原稿は2021年12月10日17時に執筆しています。}から，2021年12月31日までデータを伸ばし，推定してみようではありませんか。
Let's quickly use the same code to jump to a future schedule. We only have data up until December 6, 2021\footnote{This manuscript was written at 17:00 on December 10, 2021.}. So why don't we extend the data up to December 31, 2021, and try to make some estimates?
% コードは省略しますが，実行した結果は次のような図\ref{fig:29_07}として得られます。
The code is omitted, but the result of the execution can be obtained as the figure \ref{fig:29_07}.
\input{x29_SSpace_escape_22}

% これをみると，データが得られなくなった次の日から，確信区間が日を追うごとに広がっていく様がみて取れます。当然そうですね。$\mu_{t+1}$は$\mu_t$にプラスかマイナスか，いずれとも言えない振幅幅で揺れ動くのですから，先に行けば行くほどその可能性は不確かなものになっていくのです。ちなみに描画用のデータから2021年最後の数日間の予測値を見てみると，次のようになっています。
When you look at this, you can see that the confidence interval widens day by day from the day after the data can no longer be obtained. This is, of course, only natural. $\mu_{t+1}$ oscillates with an amplitude that could be either plus or minus from $\mu_t$, so the farther you go, the more uncertain its possibilities become. By the way, if you look at the forecast values for the last few days of 2021 from the drawing data, it looks like this.
\input{x29_SSpace_escape_23}
% 未来の状態$\mu$は82.2のあたりでほぼ固定，それに伴って想定される体重\verb|weight2|も82.2程度ですが，幅がどんどんと広くなっていってます。可能性として，80kgに落ちているかもしれませんし，84kgになっているかもしれない，というのが現時点での予測になります。
The future state μ is almost fixed at around 82.2, and the expected weight "weight2" is also about 82.2, but the range is gradually widening. Potentially, it might have dropped to 80kg, or it could have reached 84kg. That's the current forecast.

% 予測ができるとは言え，なんだかほとんど変わり映えしない数字で面白くないですね。それは当然，「明日の体重は今日の体重+誤差だろう」という，とくに何らメカニズムや仮定を入れなかったものですから，ほぼ横一線の推定にしかならないわけです。
Although it can be predicted, it's not interesting with almost the same numbers, isn't it? Naturally, this is because it's based on the premise that "Tomorrow's weight would be today's weight plus an error," which does not incorporate any particular mechanism or assumption, so it will only result in almost flat estimates.

% では少し欲張って，構造を入れみましょうか。
Shall we be a little greedy and try to incorporate some structure?
% ここまでは，$\mu_t$は$\mu_{t-1}$にのみ影響される，というモデルになっていましたが，もう1日前の影響が入っていることを考えます。
Until now, the model assumed that $\mu_t$ was only influenced by $\mu_{t-1}$. However, let's consider the impact of the day before.
% たとえば体重が増加傾向にあるとか，減少傾向にあるといった，変化を考えてみることにしましょう。すなわち，$\mu_t - \mu_{t-1} = \mu_{t-1}-\mu_{t-2}+\varepsilon$です。これは昨日から今日への変化($\mu_t - \mu_{t-1}$)は，一昨日から昨日への変化($\mu_{t-1}-\mu_{t-2}$)と同じようなものだろう(誤差$\varepsilon$はあるけど)，と考えていることになります。このようなモデルを\keytermL{2階差分のトレンド}{2かいさぶんのとれんど}と呼びます。
Let's consider changes, such as a tendency to gain weight or a tendency to lose weight. That is, $\mu_t - \mu_{t-1} = \mu_{t-1}-\mu_{t-2}+\varepsilon$. This means that the change from yesterday to today ($\mu_t - \mu_{t-1}$) is similar to the change from the day before yesterday to yesterday ($\mu_{t-1}-\mu_{t-2}$) (although there is an error $\varepsilon$). We call this type of model a "second order difference trend".

% この式を変形すると，
When we transform this expression,
\[ \mu_t = \mu_{t-1} + \mu_{t-1} -\mu_{t-2} + \varepsilon \]
\[  = 2\mu_{t-1} -\mu_{t-2} + \varepsilon \]
% となることはすぐにわかりますね。これを確率モデルで表現するなら，
It becomes clear immediately. If we represent this in a probability model,
\[ \mu_t \sim N(2\mu_{t-1}-\mu_{t-2},\tau) \]

と考えていることと同じですから，そのようにStanのコードも変形します(コード\ref{stan::29_03})。

\begin{lstlisting}[language=c++,label={stan::29_03},caption={2階差分トレンドのコード}]
...(前略)...
model{
  mu[1] ~ normal(muZero, tau);
  mu[2] ~ normal(mu[1] , tau);
  
  {
    int j = 0;
    for(l in 1:L){
      if( W[l] != 999){
          // こっちは尤度
          W[l] ~ normal(mu[l],sig);
      }else{
        j = j + 1;
        // こっちはパラメータ
        Miss_W[j] ~ normal(2*mu[l-1]-mu[l-2], sig);
      }
    }
  }
  
  for(i in 3:L){
    //2階差分
    mu[i] ~ normal(2*mu[i-1]-mu[i-2],tau);
  }
  
  muZero ~ normal(80,10); 
  sig ~ cauchy(0,5);
  tau ~ cauchy(0,5);
}
\end{lstlisting}
コードは2階差分になっているので，状態は3時点目からしか推定できません。最初の2行で$\mu_1,\mu_2$をメカニズムに沿って推定し，変化が取れるようになってから，は2階差分のコードで推定します。\verb|for|文が3から始まっていることに注意してください。

このコードと年末までのデータ，そしてプロット関数を使って推定した結果が次の図\ref{fig:29_08}になります。
\begin{figure}[ht]
   \begin{center}
      \includegraphics[width=0.8\linewidth,keepaspectratio]{../images/29_SSpace/Rplot29_05.png}
      \caption{2階差分トレンドの予測}
      \label{fig:29_08}
   \end{center}
\end{figure}

これを見ると，それまでの増加傾向を反映して，年末には84kg，最悪の場合86.6kgまで増えている可能性があることがわかります\footnote{なんてこった，まだ執筆中は答えが出ていませんが，この予測通りになったらとんでもないことですよ。だって私は20代66kgだったんです。あの頃から20kgも増えている自分の未来なんて想像したくないですよ。またダイエット始めるかなぁ。}\footnote{2021/01/15追記。2021/12/31,6:07AMの記録は82.3kg,体脂肪率26.6\%でした。ちゃんと確信区間の中に入ってますね！}。

\begin{lstlisting}[language=c++,label={code:29_06},caption={2階差分トレンドで予想される2021年末の体重(一部略)}]
> plot.tmp %>% tail()
# A tibble: 6 × 11
# Rowwise: 
   ID date       weight bodyFat   MAP   U95   L95   FLG weight2
<dbl> <date>    <dbl>   <dbl> <dbl> <dbl> <dbl> <dbl>   <dbl>
  738 2021-12-26    999      NA  83.9  85.6  81.5     2    83.6
  739 2021-12-27    999      NA  83.9  85.8  81.5     2    83.8
  740 2021-12-28    999      NA  84.0  85.9  81.4     2    83.8
  741 2021-12-29    999      NA  84.1  86.1  81.4     2    83.9
  742 2021-12-30    999      NA  84.2  86.3  81.3     2    84.1
  743 2021-12-31    999      NA  84.2  86.5  81.2     2    84.0
\end{lstlisting}

今回は2階差分のトレンドを入れての予測となりましたが，この他にも周期的に影響してくる季節項をいれるとか，たとえば「週末は体重が増加するだろう」と考えて週末効果の項を入れるとか，さまざまな工夫を思いつくことができるのではないでしょうか。

\section{状態空間モデルの展開}
最後になりましたが，状態空間モデルは表面に現れている値の背後に潜在変数を仮定し，それを滑らかに繋ぎ合わせた\keyterm{スムージング}{すむーじんぐ}{smoothing}の技術であるとも考えることができます。時系列を通じて，その前後の「状態」を，柔らかなバネで結合し，データ全体を通じて変化する関数として相互に調整させあっている，と考えることもできるでしょう。

また，時間というのは過去から未来へ進む，1次元の道のりです。ところで私たちは2次元の地図を見たり三次元の空間を見たりしていますよね。たとえばこの状態空間が，時系列的一次元ではなく，X軸とY軸に広がる網のような二次元結合していることを考えると，空間的な統計分析が可能になります。たとえば地価というのは，駅からの距離や大型商業施設，コンビニエンスストアなどの利便性に応じて変化しますが，おそらくそれらは連綿と繋がっていて，徐々に変化していくものであるはずです。であれば徐々に変化する要素をつなぎ合わせて，面の状態空間モデルを作ってやることもできるわけです。さらにその地価の変動を考える，つまり二次元平面$\times$時系列に潜在変数をつなげてスムージングしたモデルを作れば，時系列的な地価の変動を考えたり予測したりできるかもしれません。

心理学はこれまで，調査法で一時点を切り出してその構造を把握する，というアプローチが多くありました。時系列を扱うとしても，プレ・ポストの変化を見るとか，あまり長期にわたって追いかけるようなことはしてきませんでした。しかし冒頭でお話ししたように，これからの心理学データは時間的にも空間的にも広がるものを計測し，利用できるようになるかもしれません。そのためにはより深い人間の観察や推察によるモデルの作り込みと，状態空間モデルとベイズ推定のような数理モデルと強力な推定法のタッグが，必要になってくるでしょう。

\section{課題}
体重変化のデータには，体脂肪の記録もあります。体重$\times$体脂肪率から，筆者の筋肉量を計算できます。
2020年以降のデータを使って，筋肉量の変化を状態空間モデルで推定してみてください。2階差分トレンドモデルで，未来のデータも予測できればなお結構です。
これらのモデルを分析するR/Stanコードを提出してください。結果の解釈などを，スクリプトのコメントアウトや別添ファイルなどで提供してもらえるとなお良いです。もちろんRmdファイルでの提出であれば完璧です。なお提出されたコード単体でバグがなく動くことが確認できないものは，未提出扱いになります。コードの書き方などわからないところがあれば，曜日別TAか小杉までメールで連絡し，指導を受けてください。

\end{document}
