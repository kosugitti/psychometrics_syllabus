\begin{lstlisting}[language=c++,label={code:09_07},caption={データの行列演算}]
n <- nrow(dataset)
one <- rep(1, n)
m <- t(dataset) %*% one / n
V <- dataset - one %*% t(m)
S <- t(V) %*% V / n
SD <- diag(S) %>% sqrt()
Q <- diag(SD)
Z <- V %*% solve(Q)
R <- t(Z) %*% Z / n
cor(dataset)
\end{lstlisting}
