\begin{lstlisting}[language=c++,label={code:13_03},caption={因子分析モデル}]
model3 <- "
 visual =~ x1 + x2 + x3
textual =~ x4 + x5 + x6
  speed =~ x7 + x8 + x9
"
result3 <- sem(model3, data = HolzingerSwineford1939)
summary(result3, fit.measures = T, standardized = T)
\end{lstlisting}
