\begin{lstlisting}[language=C++,label={code:18_04},caption={cmdstanrパッケージによる呼び出し}]
library(cmdstanr)
model <- cmdstan_model("sevenScientist.stan")
fit1cmdstan <- model$sample(
  data = list(Y = x),
  chains = 4,
  parallel_chains = 4
)
sample <- fit1cmdstan$draws() %>% 
  posterior::as_draws_df() %>% 
  as_tibble()
\end{lstlisting}
