# Psychometrics プロジェクト

## プロジェクト概要

心理統計法のテキストとシラバスを管理するプロジェクト。

- `contents_basic/`: テキスト本文 (ch01〜ch29)
- `syllabus_basic/`: シラバス (b01〜b30)
- テキストのch番号とシラバスのb番号が対応する（例: ch05 ↔ b05）

## ベイズ章群（b25-b29 / ch25-ch29）の再編方針（2026-05-27 確定）

ベイズ教育の縦串を以下で整理：

| 章 | タイトル | 役割 | 状態 |
|---|---|---|---|
| ch25 | 3つめの推定法 | 定理の導入＋3手法の比較。モンティ・ホールは章末補遺 | **執筆済** |
| ch26 | 分布で推定する | 推定結果＝分布という転換。事前分布と尤度の折衷はここ | **執筆済** |
| ch27 | ベイズで判断する | NHST比較 / 確信区間とROPE / ベイズファクター / 利点3点 | **執筆済** |
| ch28 | JASPでベイズ分析を体験する | GUI ツールで実装に落とす | **完成**（JASP 0.98.1 に更新済み） |
| ch29 | ベイズを実践する | MCMC・確率的プログラミング言語・brms・Stan・生成量 | **完成**（Stan環境構築の補遺つき） |
| ch30 | 後期のまとめ | b30と対の試験回 | **完成** |

確定事項:

- 各種モデリング（分散検定，打ち切り，混合分布など）は基礎コースのテキスト範囲から外し，3 年次「ベイズ統計学」（半期）に委ねる
- ch29 は読み物・入口として位置づける（経験上 ch29-30 は授業時間内に収まらない）
- BayesRTMB は脚注で言及のみ（RTMB 書法を学んだあと cmdstan 書法を再学習するコストが無駄との判断）
- ch27 で PPC は添え物（ROPE 派の姿勢に整合，実装視点での PPC は ch29 で扱う）
- 「科目の中でのこのコマの位置づけ」tabular は全章で除外（旧 b04/b23/b25/b26 から削除済）

## 解決すべき課題

### 1. シラバスの項立てとテキストの章立ての一致

シラバスの `\begin{description}` 内の `\item[...]`（コマ主題細目）と、テキストの `\section{}`（章立て）が一対一対応していない章がある。

#### 不一致の章リスト

なし（全章対応済み，2026-05-27 ch25/ch26 のシラバス・本文対応の不一致を解消）

#### 対応が取れている章（参考）

- **完全一致**: ch04, ch05, ch08, ch13, ch14, ch18, ch23, ch24, ch25, ch26, ch28
- **概ね対応**: ch01-03, ch06-07, ch09-12, ch16-17, ch19-22

#### 対応不要の章

- b15, b30: 試験回（テキストなし）

### 2. ベイズ章群の本文執筆 — **完了（2026-08-23）**

ch25〜ch30 すべて執筆済み。全30章に本文が入った。2026-08-20〜23 に全章の最終校正（誤字脱字・表記ゆれ・論理・索引の4フェーズ，計461件）も完了。詳細は WORKLOG.md を参照。

以下は完了した項目の記録：

1. ~~ch27 本文の再編~~ — 完了（2026-08-07）。ch27.old の前半（MCMC・確率的プログラミング言語）は ch29 へ，「GUIソフトウェアの登場」は ch28 冒頭へ，「生成量」は ch29 へ移設。各スケルトンに `% 移設元:` で行番号を記録してある
2. **ch28 本文の見直し** — JASP の最新バージョンに合わせたスクショ・メニュー名の確認。内容は据え置きで微調整のみ
3. **ch29 本文の新規執筆（最優先ブロッカー）** — `\section{brmsによる実用的モデリング}` と `\section{stan言語を使ったプログラミング}` の2節がゼロから。前半（ベイズ法が普及しなかった理由・MCMC法の登場・確率的プログラミング言語）と「生成量による新しい仮説の立て方」は ch27.old から移設するだけ
4. **心理統計演習（m 系）の SEM 章新規執筆** — 別系統だが「書かなければならないリスト」入り

執筆ペース: まとまった時間が取れる夏休みを目処としつつ，普段のセッションでも空き時間に「ちょっと書く？」と水向けして 1 節ずつ進める。

### 3. KDP版の出し直し（V3ベースへ，夏休みの宿題）

Amazon KDPで公開中の『心理学データ解析基礎』は旧 m系（`v1_2/course_materials/tex/Dkiso1_book_kdp.tex` → `m01`〜`m30`）で，誤字と旧構成が残る。V3ベースで出し直す方針（2026-07-08確定）。

**ラッパの移植は完了した（2026-08-25）。**`contents_basic/BasicBook3_kdp.tex` を作成し，PDF/X-1a出力・twoside内外マージン・章扉のノンブル抑制・行番号なし・あとがき・奥付を移植。巻末付録は**新シラバス(b系)30本**に切り替え済み。**491ページ・エラー0でビルド確認済み**。本文は BasicBook3.tex と同じ ch*.tex を読む。

**方針: 3.30.0 でKDP版と配布版を揃えてスタートする。**通し読みの校正が終わってから。残る作業:
1. バージョンを3.30.0へ。**`compile1text.sh` はパッチを+1する仕組みしか持たない**ので，マイナーを上げるには手動対応かスクリプト拡張（`--minor` / `--major`）が要る
2. 奥付の著作権表示が `Copyright (C) 2021` のまま
3. あとがきを2本立てに（「初版にむけたあとがき」＋新規。本文は著者が執筆）

## ファイル構成

```
Psychometrics/
├── contents_basic/     # テキスト本文
│   ├── ch01_introduction.tex
│   ├── ...
│   ├── ch28_bayesian_with_jasp.tex
│   └── ch29_bayesian_modeling.tex   # スケルトン（本文未執筆）
├── syllabus_basic/     # シラバス
│   ├── b01_Introduction.tex
│   ├── ...
│   └── b30_final_exam.tex
├── GoogleFormMaker/
│   └── google-forms-mcp/             # matteoantoci/google-forms-mcp の外部 clone
└── CLAUDE.md           # このファイル
```

注: `GoogleFormMaker/google-forms-mcp/` は外部リポジトリの clone で，本リポジトリでは追跡しない（`.gitignore` 済，2026-05-27 整理）。マシンごとに個別に clone する運用。

## 索引の仕組み（2026-08-07 整備）

- `myStyle.sty` は **imakeidx** を使い，索引を2本持つ。通常の用語索引と `ridx`（R・操作用語索引）
- マクロ: `\keyterm{表示}{読み}{英語}` / `\keytermL{表示}{読み}` / 親子登録の `\keytermsub{表示}{読み}{英語}{親表示}{親読み}` と `\keytermLsub` / R索引へ送る `\keytermR` `\keytermRL`
- **第2引数は索引のソートキー**。ここに英語を書くとアルファベット節に飛ぶ（統計記号を意図的にそこへ置く場合を除く）
- **`\makeindex` には `noautomatic` が必須**。付けないと imakeidx が makeindex を自動実行し，upmendex が作った五十音見出し付きの索引を上書きしてしまう
- 索引の生成はコンパイルスクリプトの `upmendex` が担う。BasicBook3 は `tmp.idx` と `ridx.idx`，SocialPsychology は `nameidx.idx` と `termidx.idx`

## ビルド時の注意

- compile系スクリプトは **`--push` を付けたときだけ** commit/push する（既定はコンパイルのみ）
- **bibは `~/Dropbox/myBiber.bib` 一本**。`myStyle.sty` が `../../../myBiber.bib` を直接引く。独自bibの複製は作らない（2026-08-23 に統合。以前は compile が毎回 syllabus_bib.bib へコピーしていた）。`sbib.bib` は v1_2 の旧KDP版が参照しているので残置
- **中間ファイルはcompile系スクリプトが最後に消す**。`.gitignore` にも登録済みなので、章別コンパイルの副産物（ch*.log 等）がgitに入ることはない
- **KDP版(`BasicBook3_kdp.tex`)のビルドには `*_versions*.md` が要る**。`masthead.tex`(奥付)が `../../Book_versions1.md` と `../../Syllabus_versions1.md` を読んで版数を刷り込むため，Dropbox外でビルドするときにコピーを忘れると `File not found` で Emergency stop になる
- **Dropbox配下では lualatex が `Input/output error` で落ちることがある**。ディスク容量の問題ではなく，ファイルプロバイダが図版PNGの実体化に追いつかないため。落ちる位置は毎回変わる。Dropbox外にコピーしてビルドすれば通る
