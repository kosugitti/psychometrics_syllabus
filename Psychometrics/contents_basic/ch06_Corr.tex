\documentclass{ltjsbook}
\usepackage{docmute} 
\usepackage{../../myStyle} 
\begin{document}
\VerbatimFootnotes
\chapter{さまざまな相関係数とRによる実践}
\label{ch06_Corr}
\section{二変数間関係の指標}
前回は1変数の記述統計について学んできましたが，本コマからは複数の変数間の関係性について考察します。

\subsection{クロス集計表による理解}
名義尺度水準や順序尺度水準など，算術計算ができない変数同士であってもその関係性をチェックする方法はあります。\keyterm{クロス集計表}{くろすしゅうけいひょう}{contingency table}はカテゴリカルデータの関連を表現する基本的な方法です。次の例(表\ref{tab:age_sleep})を見てみましょう\footnote{説明のための仮想データです。実際のデータではありません。}。
% latex table generated in R 4.5.0 by xtable 1.8-4 package
% Sat May 17 13:32:22 2025
\begin{table}[ht]
\centering
\caption{年齢層と睡眠障害のクロス集計表} 
\begin{tabular}{rrrr}
  \hline
 & あり & なし & 合計 \\ 
  \hline
10代 & 8 & 30 & 38 \\ 
  20代 & 30 & 77 & 107 \\ 
  30代 & 27 & 60 & 87 \\ 
  40代以上 & 40 & 28 & 68 \\ 
  合計 & 105 & 195 & 300 \\ 
   \hline
\end{tabular}
\label{tab:age_sleep}
\end{table}
これは行変数として年齢層変数，列変数に「睡眠障害の有無」変数をおいたものです。
このように，2つのカテゴリカルデータを組み合わせて，それぞれの組み合わせの出現頻度を数えたものがクロス集計表です。たとえば，10代の人は38人中8人が睡眠障害があるということがわかります。これをもとに，年齢層と睡眠障害の関連を調べることができます。

特に，年齢ごとの違い(行変数の効果)が見たい場合は，睡眠障害の有無(列変数)を基準にした相対度数にするといいでしょう。
% latex table generated in R 4.5.0 by xtable 1.8-4 package
% Sat May 17 13:44:52 2025
\begin{table}[ht]
\centering
\caption{年齢層と睡眠障害のクロス集計表(列パーセント)} 
\label{tab:age_sleep_col_pct}
\begin{tabular}{rrr}
  \hline
 & あり & なし \\ 
  \hline
10代 & 7.70 & 18.90 \\ 
  20代 & 31.70 & 35.20 \\ 
  30代 & 34.60 & 30.10 \\ 
  40代以上 & 26.00 & 15.80 \\ 
   \hline
合計 & 100.00 & 100.00 \\ 
   \hline
\end{tabular}
\end{table}
このようにしてみると，10代は7\%しかない睡眠障害が30代では34\%に増え，40代以上では26\%に減っていることがわかります。

今度は，睡眠障害の有無はどういう特徴があるのか知りたいとします。睡眠障害の有無は列変数ですから，今度は行ごとに相対頻度にします。
% latex table generated in R 4.5.0 by xtable 1.8-4 package
% Sat May 17 13:47:58 2025
\begin{table}[ht]
\centering
\caption{年齢層と睡眠障害のクロス集計表(行パーセント)} 
\label{tab:age_sleep_row_pct}
\begin{tabular}{rrrr}
  \hline
 & あり & なし & 合計 \\ 
  \hline
10代 & 17.80 & 82.20 & 100.00 \\ 
  20代 & 32.40 & 67.60 & 100.00 \\ 
  30代 & 37.90 & 62.10 & 100.00 \\ 
  40代以上 & 46.60 & 53.40 & 100.00 \\ 
   \hline
\end{tabular}
\end{table}

そうすると，睡眠障害は10代ではおよそ$2:8$の比率が，40代では$5:5$の比率になっていることがわかります。つまり，年齢層が上がるにつれて，睡眠障害の有無の比率が変化していることがわかります。
このように，行の比較をするには列のパーセント，列の比較をするには行のパーセントを出して考えるのが基本です。


クロス集計表は，カテゴリカルデータの関連を調べるための基本的な手法であり，名義尺度や順序尺度の変数間の関係を把握するのに役立ちます。
しかし，連続変数同士の関係を調べる場合はどうでしょうか。たとえば，身長と体重の関係を調べたいとします。身長は連続変数，体重も連続変数ですから，クロス集計表は使えません。このような場合には，\keyterm{共分散}{きょうぶんさん}{covariance}や\keyterm{相関係数}{そうかんけいすう}{correlation coefficient}を使います。

\section{変数間関係の指標}
指標の話をする前に，可視化を考えましょう。複数の変数を図にするとき，連続的な変数であれば\textbf{散布図}を描くのが直感的にもわかりやすいでしょう。

\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,width=0.8\linewidth]{figures/06_Corr/scatter_plot.png}
	\caption{散布図の例}
	\label{fig:06_scatter_plot}
\end{figure}

% latex table generated in R 4.5.0 by xtable 1.8-4 package
% Sat May 17 14:09:59 2025
\begin{table}[ht]
\centering
\caption{2020年プロ野球選手の身長と体重のデータ(一部)}
\label{tab:06_scatter_plot}
\begin{tabular}{rrlrr}
  \hline
 & ID & Name & height & weight \\ 
  \hline
1 &   1 & 菊池　涼介 & 171.00 & 72.00 \\ 
  2 &   2 & 鈴木　誠也 & 181.00 & 98.00 \\ 
  3 &   3 & Ｋ．ジョンソン & 193.00 & 95.00 \\ 
  4 &   4 & 會澤　翼 & 177.00 & 91.00 \\ 
  5 &   5 & 大瀬良　大地 & 187.00 & 95.00 \\ 
  6 &   6 & 長野　久義 & 180.00 & 85.00 \\ 
   \hline
\end{tabular}
\end{table}

図\ref{fig:06_scatter_plot}に，身長と体重のデータをプロットしたものを示しました\footnote{このデータは第\ref{cb04_Number_and_Data}章や第\ref{ch05_descriptives}章で使ったものと同じ，2020年のプロ野球選手のデータセットです。データは全体で664人分になります}。データの一部は表\ref{tab:06_scatter_plot}に示した通りです。X軸のラベルにheight(身長)，Y軸のラベルにweight(体重)とありますように，2つの変数を同時にプロットしています。左下に目立つように1点色とサイズを変えた点を打ちましたが，これは表\ref{tab:06_scatter_plot}のID=1のケース，つまり 菊池涼介選手を表しています。彼は身長が171cm，体重が72kgであり，対応するX軸，Y軸をみるとちょうどその値になっていますね。このように，各点が1つのケースに対応しているのが散布図です。

これを見ると次のことがいえそうです。すなわち，身長が高い人は，体重も大きく，背が低い人は体重も小さいが多い傾向がある，と。というのも，身長が高くて体重が軽い領域(グラフの右下)や，身長が低くて体重が重い領域(グラフの左上)に点が打たれていないので，そういう人はいない，と思えるからです。この図のように，散布図に打たれた点が右上がりの方向にあると，2つの変数の間になんらかの関係があるということが推察できますね。その関係がどのようなものであれ，このような関係にあると私たちは「何があるんだろう」と考えたり，「背が高いということは体重も重いだろうな」と予測したりできます。来年チームにやってくる新しい選手が，どんなポジションでどんな特技を持っているかはわからなくても，身長が200cmだということだけ分かれば，まあ体重が60kgってことはないよな，と予測できますものね。

さて，前期後期を通じてこの授業のポイントは「見えないものを見ようとする」でした。皆さんはここで変数間の相関を推察したわけです。関係がありそうだと予測したと言ってもいいかもしれません。直接は見えない関係をデータから見出すには，変数間関係が重要であることがお分かりいただけますでしょうか。

図\ref{fig:06_scatter_plot}に示した関係は，とくに「正の相関関係」と言います。方向が同じ，つまり一方が大きければ他方も大きい，一方が小さければ他方も小さい，という関係にあるからです。逆に負の相関関係というのもあります。一方が大きければ他方は小さく，その逆も成り立つような状況がそうで，散布図でいうと点が右下がりになるような散らばりをするときです。負の相関関係でも関係は関係ですから，予測できますね。

どちらの傾向もないデータは相関がない，無相関である，といいます。散布図で書くと，各点が円のように広がる形になります。無相関であれば，関係性・規則性がないので，予測はできません。性格心理学では血液型と性格は無相関である，ということがわかっていますが，この話をすると「でも僕は血液型はA型で，多くの人に几帳面な性格だ，って言われるんですよ！」と反論する人がいます。実際に授業しているとよくある質問です。何がおかしいのかわかりますか？ 心理学者の主張は「無相関である」です。関係がない，一般的な傾向がないということです。無関係なので，A型で几帳面な人もいるし，几帳面でない人もいます。B，O，AB型で几帳面な人もいますし，そうでない人もいるというだけなのです。無相関であれば，そこから考察することはできません。

このように，変数間関係は色々な面において重要な指標です。分散がなければ情報が取り出せない，というのと同じで，相関関係がなければ関連する情報が取り出せないのです。このことを念頭に置いた上で，変数間関係を指標化することを考えましょう。

\paragraph{共分散}二変数間関係の強さを表す指標は\keyterm{共分散}{きょうぶんさん}{covariance}といいます。さきほどの図\ref{fig:06_scatter_plot}を例にとって考えてみましょう。身長変数を$x$，体重変数を$y$とします。ID=1の菊池選手のデータは，$x_1= 175,y_1=68$のように表します。データ全体を使って身長の平均，体重の平均を計算できますが，これをそれぞれ$\bar{x},\bar{y}$と表しましょう。共分散は次の式で表されます。
\[ s_{xy} = \frac{1}{N}\sum_{i=1}^N (x_i - \bar{x})(y_i - \bar{y}) \label{ref:covariance}\]
これはよく見ると，分散の式に似ていると思いませんか。分散の式は次のように書くのでした。
\[ s_{x}^2 = \frac{1}{N}\sum_{i=1}^N (x_i - \bar{x})^2 \]
この二乗になっているところを展開して見ると次のようになります。
\[ s_{xx} = \frac{1}{N}\sum_{i=1}^N (x_i - \bar{x})(x_i - \bar{x}) \]
こうして$s_{xy}$と$s_{xx}$を見比べるとわかりやすいと思いますが，分散は変数$x$と$x$自身との共分散であるとも言えるわけです。

もう少し違う見方をしてみましょう。共分散は\uline{それぞれの変数についての平均偏差}の積の平均である，とも言えます。$(x_i - \bar{x})$は変数$x$について，$i$さんの値が平均からどれほどずれているかを表していて，平均より上であればプラス，下であればマイナスになります。$(y_i - \bar{y})$は同様に変数$y$についての話ですが同じ個人$i$さんについてのデータでもあります。なので，ある個人$i$が，変数$x$でも平均より上，変数$y$でも平均より上であれば，この2つを掛け合わせた$(x_i - \bar{x})(y_i - \bar{y})$はプラス$\times$プラスで，プラスの数字になります。
同様に，ある人がどちらの変数でも平均より下であれば，マイナス$\times$マイナスでプラスですから，この数字は大きくなります。
この数字が小さくなるのは，ある人が一方の変数で平均より上，他方の変数で平均より下，というときです。このとき掛け合わせた数字はマイナスになるので，総和を減らすことになります。このような数字を人数ぶん足し合わせる($\sum_{i=1}^N$)という操作ですから，最終的にプラスであれば正の相関関係マイナスであれば負の相関関係がある，ということになるわけです。

注意すべきことは，共分散の大きさは変数の単位に依存するということです。たとえば身長と体重の共分散の場合ですと，身長の単位はcm，3桁の数字で，体重は単位がkg，2桁の数字が多いでしょうから，かけ合わせたもの平均は2〜3桁の数字になるでしょう。この時の単位はcm$\times$kgでよくわかりません。ただ，身長をmmに換算し，体重をインチに換算するといったことをすると，それに応じて共分散の大きさは変わってきます。関係が同じなのに，単位を変えただけで数字が変わってしまっては困りますね。


\section{相関係数：標準化された共分散}
\keyterm{相関係数}{そうかんけいすう}{correlation coefficients}は，単位に影響されずに変数間関係をみるものです。共分散の大きさは，元データの単位に依存するので，変数間関係を表す指標の大きさが単位に依存するということは，異なる変数間関係を比較できないことになって不便だからです。「身長と体重の関係の強さ」と「国語の成績と算数の成績の関係の強さ」はどっちが強い結びつきをしているのでしょう？ との問いには共分散を見ても答えられません。さてでは，それぞれの変数の単位を揃えるにはどうしたらよいのでしょう？その目的のために，\keyterm{標準化}{ひょうじゅんか}{standardize}について考えます。

\subsection{標準化}
\label{zScore}
平均値と標準偏差を使って，単位の違う変数を比較できるようにします。この手法を\keyterm{標準化}{ひょうじゅんか}{standardize}といいます。

心理学の研究対象の多くは，絶対ゼロ点を持たないものです。たとえば性格というのを考えると，とある性格特性が「ない」ということはありません。もちろん他の人とくらべてより明るい人だとか，より勤勉な人だとか，より情緒が不安定な人だ，といったことはあります。しかしこれらはいずれも，多くの人の中での相対的な位置であって，明るい人でない＝暗い人ではあっても，明るいか暗いといった次元に存在しない人，というのはありません。すなわちデータの特徴として間隔尺度水準であることがせいぜいなのです。そうした他者との比較において考える時，相対的な位置を表現する方法を考えなければなりません。

ゼロ点が存在しないので，相対的な位置を決める中心点を平均値におくとしましょう。平均値に比べてその人がどの程度離れているか，というのは平均偏差ですから$x_i - \bar{x}$で表現できます。表\ref{tbl::06_03}にとある学生の成績のデータを載せてみました。これをみると，この人の素点は英語が最も高く，この人は英語が得意なんだな，と思うかもしれません。でもクラス平均と比べてみると，あらびっくり，むしろ平均より低いので，相対的に英語は苦手なんじゃないか，ということがわかります。素点だけでは困りますよね\footnote{私の娘が通っていた中学校では，定期試験の結果を表にまとめたプリントを親に見せてくれるのですが，本人の素点と本人の平均点しか表示されておらず，まったく無意味な数値の羅列でした。学校に抗議するよう娘には伝えたのですが，卒業まで決して直ることはありませんでした。}。さて，偏差を見ると国語と数学のどちらもその平均偏差は$+14$です。なるほど，国語と数学は同じぐらい出来がいいのだな，と考えたいところですが，果たしてこの考え方もあっているでしょうか？
\begin{table}[ht]
	\centering
	\caption{成績の比較}
	\begin{tabular}{ccccc}
		科目 & 成績$X_i$ & クラス平均$\bar{X}$ & 偏差($x_i - \bar{x}$) & SD($s_x$) \\\hline
		英語 & 86      & 90             & -4                  & 8         \\
		国語 & 67      & 53             & 14                  & 10        \\
		数学 & 44      & 30             & 14                  & 5         \\
	\end{tabular}
	\label{tbl::06_03}
\end{table}
実はこれでもまだ足りません。というのも，このクラスの標準偏差をみると，国語と数学では倍ほど違いがあります。標準偏差の違いは，データの散らばり方の違いです。図\ref{fig:06_08}にプロットしてみました。このテストの点数が，左右対称の分布をしているとした時の例です。

\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,width=0.8\linewidth]{figures/06_Corr/relative_position.png}
	\caption{データの散らばりと相対的な位置}
	\label{fig:06_08}
\end{figure}
図\ref{fig:06_08}の最上段には英語の分布とこの人の位置が書いてあります。分布のピーク＝最も人が多いところは90点ぐらいのところにあり，この人はそれの左側ですから相対的にやや劣ることがわかります。二段目・三段目に国語と数学があり，相対的に優れていることがわかります。同時に，国語の分布はやや幅広く，数学は平均点が低く散らばりの幅も少ないことがわかります。国語は幅広ですから，この人よりも高い点数を取る人もいるようです。一方数学は幅が狭いため，この人よりも高い点数を取れる人がほとんどいません。ということは，かなり上位の数学力を持っていることになります。
このように，相対的な位置を考えるときはこのデータがどれぐらい広がっているかを考えなければなりません。そこで平均からの偏差を標準偏差で割り，標準偏差いくつ分はずれているのかを考えましょう。この点数を$z_i$とし，次のように計算します。

\[ z_i = \frac{(x_i - \bar{x})}{s_x}\]

このスコア$z_i$を\keyterm{標準化スコア}{ひょうじゅんかすこあ}{standardized score}と言います。もっと簡単にzスコアということもあります。このように標準化したスコアでこの人の成績をみてみますと，英語は$-0.5$，国語は$+1.4$，数学は$+2.8$となります。国語の優秀さよりも数学の優秀さのほうが際立っていることが，数字で表されましたね。

この標準化されたスコアは平均が0ですし，標準化されたスコアの分散は1になっています(標準偏差も1です)。平均を取り除いて標準偏差いくつ分なのか，という表現にしたのですから当然ですね。言い方を変えると，この数字からは平均や分散を取り除いたことで，単位の大きさに左右されずに複数の変数で比較できるようになったということです。先ほど，国語と数学の点数を比較しました。素点だと平均や分散によって変わってくるので，それを取り除いたことで比較できたのですが，これは国語が200点満点，数学が1000点満点だったりしても可能です。相対的な位置だけの問題になっているからです。他にも，身長と体重と言った単位がまったく違うものを比較できます。心理学のデータは単位がよくわからないのですが，このスコアにすると相対的にどれぐらい「明るい人」「真面目な人」なのか，ということがわかり，比較できるようになります。標準化スコアは心理学研究の中でなくてはならないものなのです。

またみなさんは\keyterm{偏差値}{へんさち}{deviation score}というのを知っていると思います。学業成績に関係してくるアレですね。偏差値は，実はこのzスコアと同じです。ただzスコアだとマイナスになったり，単位が小さくなったりするので，平均50，標準偏差10になるように変換して，
\[ \text{偏差値}= 10z_i + 50 \]
という計算で算出します。今回の例でいうと，この人の偏差値は英語で45，国語は64，数学は78ということになります。


\subsection{ピアソンの積率相関係数}
さて，この標準化されたスコアを使って，共分散の単位を整えましょう。変数$x$と$y$の相関係数は$r_{xy}$と表します。
\begin{align*}
	r_{xy} & =  \frac{1}{N}\sum_{i=1}^N (z_{x_i} - \bar{z_x})(z_{y_i} - \bar{z_y})            & \mbox{zスコアの平均は$0$}     \\
	       & = \frac{1}{N}\sum_{i=1}^N z_{x_i} z_{y_i}                                        & \mbox{zスコアの式に戻そう}      \\
	       & = \frac{1}{N}\sum_{i=1}^N \frac{(x_i - \bar{x})}{s_x} \frac{(y_i -\bar{y})}{s_y} & \mbox{$i$は分子にしか関係ないから} \\
	       & = \frac{\frac{1}{N}\sum_{i=1}^N (x_i - \bar{x})(y_i -\bar{y})}{s_xs_y}           & \mbox{分子が共分散だ}         \\
	       & = \frac{s_{xy}}{s_xs_y}                                                          &                        \\
\end{align*}

このように展開できました。相関係数は共分散をそれぞれの標準偏差で割ったもの，と考えれば良いわけですね。この数字は単位が違う2つの変数でも計算できますし，単位が整えられているので$-1 \le r_{xy} \le 1$の範囲に収まります。

\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,width=0.65\linewidth]{figures/06_Corr/correlations.png}
	\caption{さまざまな相関係数}
	\label{fig:06_10}
\end{figure}

図\ref{fig:06_10}にはさまざまな相関係数のプロットを示しました。ちなみにさきほどの野球選手の身長と体重のデータは，相関係数$r=0.645$になります。大体の散布図のイメージを持っておいてもらうことも重要ですし，何かデータを取ったら図にする，ということを忘れないでください。

\section{尺度水準に応じた相関係数}
さて，相関係数は間隔尺度水準以上の量的変数でないと計算できません。名義尺度水準や順序尺度水準の時はどうすれば良いかというと，それぞれの尺度水準に応じた相関係数を使います。名義尺度水準の変数同士の相関は\keyterm{カイ二乗}{かいにじょう}{chi-square}値を使います。順序尺度水準の変数同士の相関は\keyterm{スピアマンの順位相関係数}{すぴあまんのじゅんいそうかんけいすう}{Spearman's rank correlation coefficient}を使います。

\subsection{$\chi^2$値}
\label{sec:chi_square}
カイ二乗値は，「$\chi^2$値」と書きます。これはエックス(x)ではなく，ギリシア文字のカイ($\chi$)であることに注意してください。

$\chi^2$値は，クロス集計表からも計算できる，名義尺度水準の変数同士の関連の強さを表す指標です。たとえば，表\ref{tab:age_sleep}のようなクロス集計表があったとき，年齢層と睡眠障害の有無の関連を調べることができます。クロス集計表から$\chi^2$値を計算するには，次の式を使います。
\[
    \chi^2  = \sum_{i=1}^k \frac{(O_i - E_i)^2}{E_i} 
\]
ここで$O_i$は観測度数，$E_i$は期待度数といいます。期待度数は，行と列の値を掛け合わせて，行の合計と列の合計で割ったものとして計算でき，意味的には「もし変数同士が関係なかったら，変数の組み合わせから出現度数はこれぐらいになるに違いない」と考えられる値です。

期待度数について，簡単な例で見てみます。まずはクロス集計ではなく，単純な度数分布表を考えてみましょう。例えば，携帯のキャリア会社はどこを選んでいますか，という調査をしたとします。表\ref{tab:carrier}のような調査結果が得られたとしましょう。
\begin{table}[ht]
  \centering
  \caption{携帯キャリアの調査}
  \begin{tabular}{cc}
    キャリア & 人数 \\\hline
    A社      & 55  \\
    D社      & 32  \\
    S社      & 33    \\\hline
    合計      &  120 \\
  \end{tabular}
  \label{tab:carrier}
\end{table}

この例で，もしキャリアの選択に何の好みもないとするなら，合計の120人は均等に分けられるはずです。つまり，A社，D社，S社それぞれの期待度数は$120/3=40$人ということになります。先ほどの記号でいうなら，$E_1=40, E_2=40, E_3=40$です。さて，実際の観測度数はA社が$O_1=55$人，D社が$O_2=32$人，S社が$O_3=33$人ですから，$\chi^2$値は次のように計算できます。

\[
    \chi^2 = \frac{(55 - 40)^2}{40} + \frac{(32 - 40)^2}{40} + \frac{(33 - 40)^2}{40} = 8.45
\]

これが期待度数と観測度数の差を表す指標になります。今回は1変数の度数分布表でしたので，変数間関係の強さと言うより，「ばらつきの均等さ」の指標ということができます\footnote{$\chi^2$値をこのように1変数で使うときは，理論的な分布からの偏りを見ることになります。今回は「どのキャリアを選ぶかについて特別な好みがない」という仮定から均等な分布を期待度数としました。実際はもちろんそんなことはありませんので，「均等からずれている」すなわち「何らかの好みがあって選ばれている」と解釈することになります。期待度数は均等にしなければならない理由はなくて，例えば日本人の血液型はA:B:O:AB=4:2:3:1というのが一般的な分布として知られています。であれば調査人数をこの比率で分けたのが理論分布になるわけです。このような$\chi^2$値の使い方は，適合度の検定と呼ばれることがあります。検定についてはのちの章で詳しく取り扱います。}。

さてこのように，$\chi^2$値は1変数の分布の偏りを調べることができました。では，2変数のクロス集計表の場合はどうなるのでしょうか。たとえば先ほどの表\ref{tab:age_sleep}の例で考えてみましょう。10代の人は8人が睡眠障害があるということでした。睡眠障害の有無に年齢層の影響がなければ，その周辺度数から$105:195$の比率であるはずですから，$105/300$のはず。同様に，年齢層に睡眠障害の影響がなければ，10代の比率と同じはずですから$38/300$のはずです。これを掛け合わせると，期待度数は$105/300 \times 38/300 = 13.3$となります。実際の観測度数は8人ですから，このセルの$\chi^2$値は
\[ (8 - 13.3)^2 / 13.3 = 2.11203 \]
となります。この計算をすべてのセルに対して行なって，計算したのが表\ref{tab:age_sleep}の$\chi^2$値です。今回は$9.9627$になります。この$\chi^2$値が大きいほど，2つの変数の関連が強いことを示しますが\footnote{このような$\chi^2$値の使い方を，独立性の検定ということがあります。検定については後の章で詳しく取り扱います。}，これは総度数や分割の仕方によって変わりますので，このままでは大小関係を評価できません。

そこで，次のような指標が考えられています。

\[ V = \sqrt{\frac{\chi^2}{N \times (k - 1)}} \]

ここで$N$は全体の度数，$k$は行数または列数の小さい方を表します。この指標は0から1の範囲に収まります。0に近いほど関連がない，1に近いほど関連がある，ということになります。この指標は\keyterm{クラメールのV}{くらめーるのぶい}{Cramér's V}と呼ばれています。これは相関係数と違って，最も小さい値は$0$，最も大きな値は$1$であることに注意してください。

\subsection{スピアマンの順位相関係数}
つぎに順序尺度水準のばあいを考えてみましょう。このような場合は\keyterm{スピアマンの順位相関係数}{すぴあまんのじゅんいそうかんけいすう}{Spearman's rank correlation coefficient}を使います。

対応する二つの変数を$x$と$y$とします。これらの変数は順序尺度水準ですから，それぞれの変数の値を順位に置き換えます。たとえば身長が170cm，体重が60kgの人は，身長では1位，体重では2位だったとします。この場合，身長の順位は1，体重の順位は2となります。これをそれぞれ$x_i$と$y_i$とします。
このとき，スピアマンの順位相関係数$r_s$は次のように計算できます。
\[
    r_s = 1 - \frac{6\sum_{i=1}^N d_i^2}{N(N^2 - 1)}
\]

ここで$d_i$は順位の差を表します。たとえば身長が170cm，体重が60kgの人は，身長では1位，体重では2位だったとします。この場合，$d_i = 1 - 2 = -1$となります。これをすべての人について計算して，$d_i^2$を足し合わせます。また，同着順位があった場合はその順位を平均して計算します。例えば，9点，8点，8点，7点という4人がいた場合，順位を「1位，2位，2位，4位」とすることになりますが，平均順位とは「1位，2．5位，2．5位，4位」とするものです。これを計算して，$d_i^2$を足し合わせます。あとは式に代入して計算すればOKです。

数式は面倒ですが，同順位が無ければ本質的に「順序の値に対してピアソンの相関係数を計算している」と考えて構いません。また，Rで実行する上では，相関係数の関数にオプションを追加するだけですので，それほど難しくはありません。

\subsection{統計モデルに基づいた相関係数}
ここまでは，変数間の関係を表す指標として，ピアソンの相関係数やスピアマンの順位相関係数を紹介しました。これらは変数間の関係を表す指標として一般的ですが、実際のデータ分析ではより複雑なモデルに基づいた相関係数が用いられることがあります。

詳細は省略しますが，これらについてもRのパッケージを使って計算することができます\footnote{ポリコリック，ポリシリアル，テトラコリック相関は，心理系の便利関数がいっぱい詰まった\verb|psych|パッケージで計算できます。小杉が作ったテストデータ分析パッケージ\verb|exametrika|でも一部対応しています。MICについては}。表\ref{tab:correlation_types}に一覧を示しますので，興味のある方は調べてみてください。大事なのは，尺度水準やデータに合った相関係数を選ぶことです。明らかに順序尺度水準でしかないのに，ピアソンの相関係数を使うのはおかしいですし，逆に連続変数であるのにスピアマンの順位相関係数を使うのもおかしいのです。データの性質をよく考えて，適切な相関係数を選ぶようにしましょう。


\begin{table}[ht]
  \centering
  \caption{特殊な相関係数の一覧}
  \begin{tabular}{p{3.5cm}p{2cm}p{2cm}p{5.5cm}}
    \toprule
    \textbf{名称} & \textbf{和名} & \textbf{対象データ} & \textbf{簡単な説明} \\
    \midrule
     Tetrachoric correlation(テトラコリック相関係数) & 四分相関 & バイナリ$\times$バイナリ & 2つの二値変数の背後に連続的な潜在変数があると仮定して算出する相関係数。2$\times$2分割表データから潜在的な連続変数間の相関を推定する。 \\
    \addlinespace
     Polychoric correlation(ポリコリック相関係数) & 多分相関 & 順序$\times$順序 & テトラコリック相関の拡張版。2つの順序カテゴリ変数の背後に連続的な潜在変数があると仮定して算出する相関係数。 \\
    \addlinespace
     Polyserial correlation(ポリシリアル相関係数) & 多系列相関 & 連続$\times$順序 & 連続変数と順序カテゴリ変数の間の相関係数。順序変数の背後に連続的な潜在変数があると仮定して算出する。 \\
    \addlinespace
    バイシリアル相関係数(Biserial correlation)  & 二系列相関 & 連続$\times$バイナリ & 連続変数と二値変数の間の相関係数。二値変数の背後に連続的な潜在変数があると仮定して算出する。点双列相関よりも推定精度が高いが、より強い仮定が必要。 \\
    \addlinespace
    Mutual Information Coefficient (MIC) & 相互情報量係数 & あらゆる形式 & 情報理論に基づく非線形関係の測度。2つの変数間の関係の強さを表し、線形・非線形を問わずあらゆるパターンの関連を0〜1の値で示す。 \\
    \bottomrule
  \end{tabular}
  \label{tab:correlation_types}
\end{table}

\section{Rによる相関分析の実践}

では実際にRで計算してみましょう。まず例によって，RStudioを立ち上げ，この授業のプロジェクトフォルダを開いてください。サンプルデータがワーキングディレクトリに入っているかを確認し，新しいスクリプトファイルを立ち上げ，いつもの呪文を書きます。この段階で「何言ってるの?」とか「うまくできない！」という人はすぐにTAを呼んでください。

\begin{lstlisting}[language=c++,label={code:06_01},caption={データの準備}]
# Rのプロジェクトを立ち上げ，プロジェクトフォルダを開いたうえで
# 以下のコマンドを実行してください
rm(list=ls())
library(tidyverse)
# データを読み込む
dat <- read_csv("BaseballDecade.csv")
# データを一部絞り込みます
dat <- dat[dat$Year == "2020年度",]
# または
# dat <- dat %>% filter(Year == "2020年度")
\end{lstlisting}

\paragraph{コード解説}
\begin{description}
    \item[3,4行目] 環境のリセットとtidyverseパッケージの読み込み
    \item[6行目] データを読み込みます。データはCSV形式で保存されていますので，\verb|read_csv|関数を使います。
    \item[8行目] データを絞り込みます。2020年度のデータだけを取り出します。データは2020年度のものだけですので，この行は必要ありませんが，Rの使い方の練習として書いておきます。
    \item[9行目] 上の行と同じ意味ですが，tidyverseパッケージの関数を使った書き方です。これを使うと，データフレームの中から条件に合ったデータだけを取り出すことができます。
\end{description}

\paragraph{散布図の作成（Rの基本関数）}
まずはRの基本関数\verb|plot|を使って、身長と体重の散布図を作成してみましょう。

\begin{lstlisting}[language=R,label={code:06_02},caption={R baseによる散布図の作成}]
# 身長と体重の散布図（R base）
plot(dat$height, dat$weight,
     xlab = "身長(cm)", ylab = "体重(kg)",
     main = "身長と体重の散布図")
\end{lstlisting}

\paragraph{コード解説}
\begin{description}
    \item[2行目] \verb|plot|関数でX軸に身長、Y軸に体重を指定します。
    \item[3,4行目] \verb|xlab|と\verb|ylab|で軸ラベル、\verb|main|でタイトルを指定しています。
\end{description}

このコードを実行すると，Rの標準グラフィックでシンプルな散布図が描画されます(図\ref{fig:06_scatter_plot2})。
\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,width=0.8\linewidth]{figures/06_Corr/Scatter_base.png}
	\caption{身長と体重の散布図（R base）}
	\label{fig:06_scatter_plot2}
\end{figure}

\paragraph{散布図の作成（ggplot2）}
次に，tidyverseに含まれている\textbf{ggplot2}パッケージを使うと，より見栄えの良い散布図を簡単に作成できます。

\begin{lstlisting}[language=R,label={code:06_03},caption={ggplot2による散布図の作成}]
# ggplot2による散布図
ggplot(dat, aes(x = height, y = weight)) +
  geom_point() +
  labs(x = "身長(cm)", y = "体重(kg)",
       title = "身長と体重の散布図（ggplot2）") +
  theme_bw()
\end{lstlisting}

\paragraph{コード解説}
\begin{description}
    \item[2行目] \verb|ggplot|関数でデータフレーム\verb|dat|を指定し，\verb|aes|でX軸に身長，Y軸に体重を指定します。
    \item[3行目] \verb|geom_point|で散布図を描画します。
    \item[4,5行目] \verb|labs|で軸ラベルとタイトルを指定します。
    \item[6行目] \verb|theme_bw|で白黒のシンプルなテーマを適用します。
\end{description}

このコードを実行すると，より美しくカスタマイズしやすい散布図が得られます(図\ref{fig:06_scatter_plot3})。
\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,width=0.8\linewidth]{figures/06_Corr/Scatter_ggplot2.png}
	\caption{身長と体重の散布図（ggplot2）}
	\label{fig:06_scatter_plot3}
\end{figure}

\paragraph{ピアソンの相関係数の計算}
ピアソンの相関係数は、2つの連続変数間の線形関係の強さを表す指標でした。Rでは\verb|cor|関数を使うと簡単に計算できます。

\begin{lstlisting}[language=R,label={code:06_04},caption={ピアソンの相関係数の計算}]
# ピアソンの相関係数の計算
cor(dat$height, dat$weight)
# または
cor(dat$height, dat$weight, method = "pearson")
\end{lstlisting}

\paragraph{コード解説}
\begin{description}
    \item[2行目] \verb|cor|関数で身長と体重のピアソンの相関係数を計算します。デフォルトではピアソンの相関係数が計算されます。
    \item[4行目] 明示的に\verb|method = "pearson"|を指定することもできます。
\end{description}

このコードを実行すると，身長と体重のピアソンの相関係数が計算されます。\verb|0.645125|という値が出力されていればOKです！

\paragraph{スピアマンの順位相関係数の計算}
今度はスピアマンの順序相関係数を見てみましょう。

まず，データを加工します。データの中から，年俸が高い野手TOP20人を抜き出して，年俸とホームラン本数の順序相関を見ることにします。
\begin{lstlisting}[language=R,label={code:06_05},caption={年俸が高い野手TOP20の抽出}]
  # データを読み込む
  dat <- read_csv("BaseballDecade.csv")
  
  # 野手（投手以外）を選び、年俸で降順にソートしてTOP20を取得
  top20_players <- dat %>%
    filter(Year == "2020年度") %>%  # 2020年度のデータ
    filter(position != "投手") %>%   # 投手以外（野手）を選択
    arrange(desc(salary)) %>%        # 年俸の降順でソート
    head(20) %>%                     # 上位20人を取得
    select(Name, team, salary, HR)   # 必要な列だけを選択
  \end{lstlisting}
  
  \paragraph{コード解説}
  \begin{description}
      \item[1行目] データファイルを読み込みます。\verb|read_csv|関数はtidyverseパッケージの関数で，CSVファイルを読み込むためのものです。
      \item[3行目] パイプ演算子\verb|%>%|を使って，データの加工を順番に行っていきます。
      \item[4行目] \verb|filter|関数で2020年度のデータだけを抽出します。
      \item[5行目] 投手以外の選手（野手）を選択します。\verb|!=|は「等しくない」という意味の演算子です。
      \item[6行目] \verb|arrange|関数で年俸の降順にソートします。\verb|desc|は降順を指定する関数です。
      \item[7行目] \verb|head|関数で上位20人を取得します。
      \item[8行目] \verb|select|関数で必要な列だけを選択します。ここでは選手名，チーム，年俸，ホームラン数を選択しています。
\end{description}

ヒストグラムを描けば一目瞭然ですが，年俸のデータは左に大きく歪んでいますし，HRもよく打つひととそうでない人がいますので，データは偏ります。
散布図を描けば一目瞭然ですが，歪んだデータ同士の相関をピアソンの相関で計算するのはよくないです。後で説明しますが，ピアソンの相関係数は外れ値に大行く影響されるからです。

順序尺度水準として捉えて，相関係数を算出するには次のようにします。\verb|0.2858256|という値が出力されていればOKです！
\begin{lstlisting}[language=R,label={code:06_06},caption={スピアマンの相関係数の計算}]
  cor(top20_players$salary, top20_players$HR, method = "spearman")
\end{lstlisting}

\paragraph{カイ二乗値の計算}
カイ二乗値の計算例として，チームと血液型の関係を見てみましょう。阪神タイガース，読売ジャイアンツ，広島カープの3チームの選手の血液型の分布を調べます。

\begin{lstlisting}[language=R,label={code:06_07},caption={チームと血液型のクロス集計表の作成}]
# 3チームと血液型変数を抜き出し、それぞれをFactor型にした上で、
# クロス集計表をだす
team_blood <- dat %>%
  filter(team %in% c("Tigers", "Giants", "Carp")) %>%
  select(team, bloodType)

# Factor型に変換
team_blood$team <- factor(team_blood$team)
team_blood$bloodType <- factor(team_blood$bloodType)

# クロス集計表の作成
cross_table <- table(team_blood$team, team_blood$bloodType)
print(cross_table)
\end{lstlisting}

\paragraph{コード解説}
\begin{description}
    \item[2-4行目] \verb|filter|関数で3チームのデータだけを抽出し，\verb|select|関数でチームと血液型の列だけを選択します。
    \item[6-7行目] \verb|as.factor|関数を使って，チームと血液型の変数を因子型に変換します。
    \item[9-10行目] \verb|table|関数でクロス集計表を作成し，\verb|print|関数で表示します。
\end{description}

このコードを実行すると，3チームの選手の血液型の分布を示すクロス集計表が得られます。この表から，チームによって血液型の分布が異なるかどうかを調べることができます。
$\chi^2$値を算出するには次のようにします。

\begin{lstlisting}[language=R,label={code:06_08},caption={カイ二乗値の計算}]
chi_sq_test <- chisq.test(cross_table)
print(chi_sq_test)
\end{lstlisting}

\paragraph{コード解説}
\begin{description}
    \item[1行目] \verb|chisq.test|関数を使ってクロス集計表からカイ二乗値を計算します。
    \item[2行目] 計算結果を表示します。
\end{description}

このコードを実行すると，チームと血液型の関係を示す$\chi^2$値が得られます。この値が大きいほど，チームと血液型の間に関連があることを示します。
出力は次のようになっているでしょう。

\begin{Rscreen}{出力例}{out06_01}
\begin{verbatim}
	Pearson's Chi-squared test

data:  cross_table
X-squared = 12.878, df = 8, p-value = 0.1161
\end{verbatim}
\end{Rscreen}

ここに表示されている\verb|X-squared = 12.878|が$\chi^2$値です。テストと書いてあるのは統計的検定の結果ですが，これについてはまたの機会に解説することにします。

\paragraph{クラメールのVの計算}
カイ二乗値は，データの大きさや分割の仕方によって値が変わってしまうため，そのままでは比較が難しいという問題があります。そこで，クラメールのVという指標が使われます。クラメールのVは0から1の範囲に収まり，値が大きいほど関連が強いことを示します。

\begin{lstlisting}[language=R,label={code:06_09},caption={クラメールのVの計算}]
chi_sq_value <- chi_sq_test$statistic
n <- sum(cross_table)  # サンプルサイズ
r <- nrow(cross_table)  # 行数
c <- ncol(cross_table)  # 列数
min_rc <- min(r, c)     # r と c の小さい方

# クラメールのVの計算
cramers_v <- sqrt(chi_sq_value / (n * (min_rc - 1)))
\end{lstlisting}

\paragraph{コード解説}
\begin{description}
    \item[1行目] 先ほど計算したカイ二乗検定の結果から，$\chi^2$値を取り出します。
    \item[2行目] クロス集計表の全セルの合計からサンプルサイズを計算します。
    \item[3-4行目] クロス集計表の行数と列数を取得します。
    \item[5行目] 行数と列数の小さい方を取得します。
    \item[7行目] クラメールのVの計算式に従って値を計算します。
\end{description}

このコードを実行すると，チームと血液型の関係の強さを示すクラメールのVが得られます。この値は0から1の範囲に収まり，値が大きいほど関連が強いことを示します。
今回は\verb|0.1981431|という数字になったはずです。チーム編成と血液型にはそれほど強い関係がない，ということができます。

\section{相関の落とし穴と可視化の重要性}
さて，ここまでいろいろな相関係数や関係する指標を見てきました。
一般にはピアソンの積率相関係数が最もよく使われます。データは量的に得られることが多いので，「ピアソンの積率」という言葉がついてないただの「相関係数」の場合は，一般にピアソンの相関係数を出していると言っていいでしょう。
しかし，深く考えずに数値だけ見ることは大変危険です。ここでは相関係数を見る時の注意すべき点をいくつかあげていきます。

相関係数を見て考える話に入る前に，注意喚起から。
表\ref{tbl:06_01}のようなデータセットがあったとしましょう\footnote{このデータセットは，Rの\verb|datasauRus|パッケージ\parencite{matejka2017same}の一部です。}
\begin{table}[ht]
	\caption{さまざまなデータセット}
	\label{tbl:06_01}
	\begin{tabular}{lrrrrrrrrr}
		\hline
		dataset & Mean.X & Mean.Y & SD.x   & SD.y   & Min.x  & Min.y  & Max.x  & Max.y  & Corr   \\\hline
		circle  & 54.267 & 47.838 & 16.760 & 26.930 & 21.864 & 16.327 & 85.665 & 85.578 & -0.068 \\
		dino    & 54.263 & 47.832 & 16.765 & 26.935 & 22.308 & 2.949  & 98.205 & 99.487 & -0.064 \\
		star    & 54.267 & 47.840 & 16.769 & 26.930 & 27.025 & 14.366 & 86.436 & 92.215 & -0.063 \\    \hline
	\end{tabular}
\end{table}
データセット，\verb|circle|も\verb|dino|も\verb|star|も，2つの変数X,Yを持っているのですが，いずれも平均，標準偏差にほとんど違いがありません。相関係数も-0.06となっており，ほぼ無相関，関係がなさそうです。

ところがどっこい，これらの変数をプロットしてみたのが図\ref{fig:06_02}です。ご覧の通り，無関係ではなくプロットすると図が見えてきましたね。
\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,width=0.8\linewidth]{figures/06_Corr/datasaurs.png}
	\caption{プロットしてびっくり}
	\label{fig:06_02}
\end{figure}

実際のデータではこんなことはないと思いますが，これは「数字だけ見ていてもわからないよ」という警告です。たくさんのデータを手に入れると，ついつい計算機で一気に数字を出してみたくなります。実際のデータ分析をするときは(あまりにも簡単に相関係数が計算できてしまうがために)，見落とされるところでもあります。数値化は楽ですが，その分なにかを見落としているかも知れない，ということについて常に注意するようにしましょう。

以下では相関関係があったとしても，注意深く考えないといけない例を挙げておきます。

\paragraph{落とし穴1；直線じゃないかも}
図\ref{fig:06_10}の左上には$r=-1$の例，右下には$r=+1$の例があります。このように，相関係数はその極限である$\pm1$のときに，直線になる関係であることを理解しておいてください。言い換えると，相関係数はあくまでも変数間の直線的関係を指標化したものであって，曲線的な関係の場合はうまく表現できないのです。図\ref{fig:06_11}には曲がった散布図の例を示しました。このデータの相関係数は$r=05$です。ほとんど関係ないですね！じゃあこのX軸の変数とY軸の変数は何の関係もない，といっていいでしょうか。たとえばこのデータ，X軸がコーヒーの温度，Y軸が美味しさだったらどうでしょう。熱すぎても冷たすぎても，コーヒーは美味しくありません。適温というのがあるのです。あるいはX軸が血圧，Y軸が健康度だったらどうでしょう。血圧も高ければ高いほど健康，低ければ低いほど不健康というわけではないですよね。これらの例にあるように，心理的なデータや整理的なデータというのは，中庸であることが重要であることがあります。その時に相関係数を算出して，それがとても低いので「血圧と健康には関係がない」と結論づけるのは間違えています。直線性を表す指標がよくない，というだけなのですから。

\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,width=0.65\linewidth]{figures/06_Corr/U_curve.png}
	\caption{曲がった関係には向かない}
	\label{fig:06_11}
\end{figure}

\paragraph{落とし穴2；全体の傾向じゃないかも}
逆に，ある2変数の相関係数が$r=0.922$でした。この2変数には強い相関があるといって良いでしょうか。待って待って，いったん落ち着いて散布図を書いてみましょう。たとえば図\ref{fig:06_12}のようなデータなのかもしれないのですから。
\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,width=0.65\linewidth]{figures/06_Corr/outlier.png}
	\caption{外れ値がひっぱる}
	\label{fig:06_12}
\end{figure}
図\ref{fig:06_12}は，確かに相関係数は高いのですが，明らかにおかしなポジションにいるデータが1件ありますよね。平均値が外れ値に引っ張られたように，平均値をその計算新規の中に取り込んでいる相関係数も，外れ値に弱いところがあります。今回この極端な値を取る1ケースを取り除くと，相関係数は$r=0.3$にまで下がります。左の小さな群だけで見れば，むしろ関係ないと考えるべきなのです。これもデータを図にしなければ気づかない落とし穴です。

\paragraph{落とし穴3；データの一部が切り取られている}
とある大学の入学試験のデータを取ってみたとします。入試の成績と，入学後の成績のデータを集め，入試の成績がどれぐらい入学後の成績と関係があるのかを考えたい，というわけです。良い成績で入学したら，入学後も成績が高いというのが普通に考えられる結果ですが，実はその相関係数は$r=0.4$ぐらいだった，ということもあります。なぁんだ，入試の成績はその後の頑張りと関係ないんじゃないか，入試なんか意味がないんじゃないか，と思うのは大間違い。

さて図\ref{fig:06_13}には，X軸に入試の成績を，Y軸には入学後の成績をプロットしてみました。相関係数は全体で0.7あるのです。ところが，大学入試というのは世知辛いもので，ある点数を取らない人はそもそもその大学に入れませんよね。図でいうと，ある線(60点)より右側の黒いデータしか分析する時に手に入らないのです。この黒い点だけみて相関係数が低いから，関係ないんだと思ってしまうのは間違いです。このように，データがそもそも打ち切られている可能性にも注意が必要です。このようなデータは切断データといわれます。これは相関係数の問題というより，データの問題なのですが，データの性質をよく考えて考察しなければならないということはしっかり意識しておいていただきたいところです。

\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,height=0.4\linewidth]{figures/06_Corr/setudan.png}
	\caption{打ち切りデータの例}
	\label{fig:06_13}
\end{figure}

\paragraph{落とし穴4；データは色々な情報が混在している? }
図\ref{fig:06_14}の左を見てください。これは右上がりの散布図ですから，なるほど変数間に関係はありそうです。実際に相関係数を計算すると$r=0.67$になります。今度は散布図もみているからバッチリ！と思うかもしれませんが，さにあらず。
実はこのデータ，3つのグループのデータが混ざった全体的な傾向です。図の右側には，グループごとに色分けをしてみました。おや，これを見るとグループの中ではむしろ右下がりの分布をしていませんか？ 実はその通りで，グループAは$r_A=-0.29$，グループBは$r_B=-0.42$，グループCは$r_C=-0.35$と，いずれも負の相関をしているのです。にもかかわらず，全体で見ると正の相関をしている，ということもあるわけですね。

\begin{figure}[ht]
	\centering
	\includegraphics[keepaspectratio,height=0.4\linewidth]{figures/06_Corr/HLM.png}
	\caption{階層的なデータ}
	\label{fig:06_14}
\end{figure}

このデータ，たとえばX軸がゲームで遊ぶ時間，Y軸が学校の成績だったとしますと，全体で見るとゲームをすれば成績が上がる，という結論になってしまいますね。でも学校別(グループ別)でみるとむしろ逆，ここにあるのは3つの学校のそもそもの成績の違いが反映されているだけでした，ということもあるわけです。このように，外的な指標で群が変わることがわかっているなら，プロットする時にその情報を加えて，このような情報の混在が生じていないかをチェックする必要があります。プロットするのは当然ですが，プロットのやり方にも注意が必要なのですね。また，群ごとの違いがわかっていなくても，潜在的に何か違うグループに分かれているのかもしれません。回答者の特性を考慮して，いくつかのクラスターに分けて分析するという手法も統計的に考えられています。データを色々な角度から見る，という姿勢は忘れてはいけません\footnote{マーケティングの領域では，セグメンテーションと呼ばれることもあります。購買層のことを指します。クラスターというのはCOVID-19の関係で一般的な用語になりましたが，この言葉そのものに悪い意味はなく，塊という意味です。}。

\paragraph{疑似相関}

さて，いろいろ「気をつけないといけない」相関係数の話をしてきましたが，最後にたまたま相関があるだけ，という可能性も指摘しておきます。

たとえば次の変数間には高い相関があることがわかっています\footnote{参考；\url{http://tylervigen.com/spurious-correlations}}。
\begin{itemize}
	\item ニコラス・ケイジ映画出演数とプール溺死数
	\item マーガリン消費率とメイン州の離婚率
	\item 自分のベッドシーツに絡まって死んだ人の数とチーズ消費量
\end{itemize}

このように，相関があるからと言って必ず意味があるわけではありません。おそらくニコラス・ケイジ(ハリウッド俳優)が頑張って映画に出たからといって，米国でのプール溺死者数が増えるわけではないし，マーガリンを塗らなければ離婚率が下がるというものでもないでしょう。ただデータを取ると，見かけ上，相関係数が高くなってしまっただけなのです。

こうした関係のことを\textbf{疑似相関}と言います。我々は目に見えない心理学的な現象を，相関関係を手掛かりに分析していくのですが，たまたまみられる表面的な関係性を「ほらやっぱり！」と思い込んでしまわないように，慎重に考えなければなりません。

\section{課題}
今回の課題も，以下の計算をするRスクリプトファイルを提出してもらいます。
ファイル名は学籍番号と氏名を含むものとし，拡張子は\verb|.R|にしてください。ただし，前回と同じファイル名にすると区別がつかないでしょうから，ファイル名の先頭に\verb|R6_|をつけてください。たとえば，学籍番号が12345678で，氏名が山田太郎の場合は\verb|R6_12345678_yamada.R|という名前になります。
また，\textbf{提出するファイルはエラーのない完備な.Rファイルにしてください。}
ファイル形式の間違い，出力にエラーが含まれている(未完成な)ものは返却しますので注意してください。

課題の内容は次のとおりです。
\begin{enumerate}
\item 2020年度のデータを対象に、本塁打数(HR)と安打数(Hit)の間のピアソンの相関係数を計算し，小数点第3位を四捨五入して第2位まで報告してください。ただし，欠測値が少なくありませんので，\verb|cor|関数の引数に\verb|use = "pairwise.complete.obs"|を指定してください。
\item 2020年度のデータから、打数(AtBats)の上位30人のデータを抽出し，
  その30人について、打数(AtBats)とヒット数(Hit)の間のスピアマンの順位相関係数を計算してください。
  計算結果を小数点第3位まで四捨五入して報告してください。
\item 2020年度のデータから、Giants、DeNA、Dragonsの3チームと血液型(bloodType)のデータを抽出し，
チーム変数と血液型変数をともにFactor型に変換したうえで，上記の2変数を用いてクロス集計表を作成するコードを書いてください。
\item 課題3で作成したクロス集計表を用いて、チームと血液型の独立性に関する$\chi^2$値を計算してください。
\item 課題4で得られたカイ二乗値を用いて、クラメールのVを計算してください。
\end{enumerate}

\end{document}