\documentclass{ltjbook}
\usepackage{docmute} 
\usepackage{../../myStyle} 
\begin{document}

% \chapter{Rによる分散分析}
\chapter{Variance Analysis using R}
\label{m22_anovaOnR}
% ここまでで，要因計画を線形モデルとして，あるいは帰無仮説検定の枠組みで考えるやり方を説明してきました。
So far, we have explained how to consider factor planning as a linear model or within the framework of null hypothesis testing.
% 細かい計算はRで行う，ということで先送りしてきた話をここで回収したいと思います。Rを使った演習でどのように計算が進むのか，どのように結果が表示されるのか，どのようにその結果を解釈するのかについて学んでいきましょう。
I would like to return to the discussion that we have postponed under the premise that detailed calculations are done in R. Let's learn how the calculations proceed, how the results are displayed, and how to interpret those results in the exercises using R.
% \section{Rで分散分析を行う上でのポイント}
You didn't provide a Japanese text for me to translate into English. Please provide a Japanese text.
% それではRを起動するところから始めましょう。復習がてら，作業を始める前の最初のステップについて改めて書いておきます。
Let's start by launching R. As a review, I will write again about the first step before starting work.
% \paragraph{RStudioの起動}RStudioを起動してください。パッケージのインストールをするときなど，管理人権限が必要になるケースがあります。
\paragraph{Launching RStudio} Please launch RStudio. There may be cases, such as when installing packages, where administrator rights are required.
% \paragraph{プロジェクトを開く}プロジェクトは前回作成した物で良いと思います。同じフォルダの中で，スクリプトファイルだけ変えれば良いでしょう。メニューからプロジェクトを開き(FIle$\to$Open Project)，RStudioの右上などで，プロジェクトが開いていることを確認しましょう($\to$図\ref{fig::20_01})。
\paragraph{Open the Project} I think it's fine to use the project that was created last time. In the same folder, you only need to change the script file. Open the project from the menu (FIle$\to$Open Project), and confirm that the project is open at the top right of RStudio, etc. ($\to$Figure\ref{fig::20_01}).
% \paragraph{Rスクリプトを開く}今日のコードを書くためのRスクリプトファイルを準備します。\verb|File > NewFile > R Script|と進み，何も書いてないRスクリプトの画面を表示させてください。真っ白いファイルが開いたら問題ありません。
\paragraph{Open the R script} Prepare the R script file to write today's code. Please proceed with \verb|File > New File > R Script|, and show the screen of the R script without writing anything. If a blank file opens, there are no problems.
\input{m22_anova_onR_escape_01}

% さて，準備OKですね。
Alright, are you ready?

% \subsection{数のタイプ}
\subsection{Types of Numbers}
% 今回は要因計画をRで計算させるということですが，ここでポイントになるのが変数の型です。
The point here is the type of variable, as we are going to have the factorial design calculated in R this time.
% 繰り返しお話ししていますように，前期でやった回帰分析と今回扱う要因計画とは，線形モデルという同じ形をしています。違うところがあるとすれば，回帰分析の説明変数が連続的であるのに対し，要因計画の説明変数は離散的(カテゴリカル)であるということです。ですから，Rで分析する時も関数の形は同じであっても，変数の種類が違うということを明らかにしておかなければなりません。
As I've been repeating, the regression analysis we did in the first half and the factorial design we deal with this time have the same form known as linear models. The difference is while the explanatory variables in regression analysis are continuous, the explanatory variables in factorial design are discrete (categorical). Therefore, even when analyzing with R, even if the form of the function is the same, we must make clear that the types of variables are different.

% Rにはいろいろな数字のタイプがあります。オブジェクトにいろいろな変数を格納できますが，その格納された変数のタイプには，次のような種類があります。
There are various types of numbers in R. Although you can store various variables in an object, there are the following types of stored variables.
\input{m22_anova_onR_escape_02}
% これらの型を確認するには，オブジェクトの仕組みがどうなっているかを調べる必要があります。調べるのは\texttt{str}関数で，これを使うとオブジェクトの中身がわかるようになっています。
To verify these types, it is necessary to inspect how the object mechanisms are constituted. We can check this using the \texttt{str} function, which allows us to understand the contents of an object.

% 第\ref{m04_usingR}講のことを思い出しつつ，\texttt{code:\ref{code:23_01}}を入力してみてください。
Please try to enter \texttt{code:\ref{code:23_01}}, remembering what you learned in lecture \ref{m04_usingR}.
\input{m22_anova_onR_escape_03}
% \paragraph{コード解説}
\paragraph{Code Explanation}
\input{m22_anova_onR_escape_04}

% さて，最後の7行目を実行すると，コンソールに次のように出力されていると思います(\texttt{Rの出力\ref{RS:out23_01}})。
Now, when you run the final 7th line, I think it is output on the console as follows (R's output \ref{RS:out23_01}).
\input{m22_anova_onR_escape_05}
% ここにあるように，\texttt{obj}オブジェクトの\texttt{name}変数はchr型で，他の3つの変数は数値型(numeric)である，というのが示されています。ここで性別\texttt{gender}を因子型にしてみましょう(\texttt{code:\ref{code:23_02}})。
As shown here, the \texttt{name} variable of the \texttt{obj} object is of type chr, and the other three variables are numeric. Let's try to make the sex \texttt{gender} a factor type (\texttt{code:\ref{code:23_02}}).

\input{m22_anova_onR_escape_06}

% これの出力は\texttt{Rの出力\ref{RS:out23_02}}のようになります。
The output of this will be similar to the output of \texttt{R\ref{RS:out23_02}}.
\input{m22_anova_onR_escape_07}
% ここに示されているように，性別がファクター型になり，それは2つの水準を持っている。それぞれに「male」「female」というラベルがついている，ということがわかります。
As shown here, the gender is a factor type and it has two levels. It is clear that each has a label called "male" and "female".

% このfactor型にしておくというのは，Rが名義尺度水準の変数なんだなと理解したことになりますので，これで回帰分析をするとOKということになります。逆にfactor型にしておかないと，量的変数として普通の回帰分析をしてしまうので，正しくない結果になります\footnote{具体的には，3水準に1,2,3という量的な数字を割り振って分析するということは，$1<2<3$という順序性と2-1=3-2という等間隔性を持たせたことになります。要因計画の可視化の時に，横軸は入れ替え可能(順序性がない)だったことを思い出してください。}。
Keeping this as a factor type implies that R understands it as a nominal scale variable, so it is ok to conduct regression analysis with this. On the contrary, if you don't keep it as a factor type, you will perform a regular regression analysis as if it's a quantitative variable, leading to incorrect results. Specifically, by assigning quantitative numbers 1, 2, 3 to three levels and analyzing them, it implies that an order of $1<2<3$ and an equidistance of 2-1=3-2 has been introduced. Please remember when visualizing the experimental design, the horizontal axis could be switched (without any order).

% \subsection{分散分析用の関数を読み込もう}
\subsection{Let's Load the Function for Analysis of Variance}
% factor型にして分析するだけで，効果の大きさは見積もれるのですが，それを「判定」するには帰無仮説検定の枠組みで論じる必要があるでしょう。
You can estimate the magnitude of the effect by just analyzing it in factor type, but to "determine" it, you would need to discuss it within the framework of a null hypothesis test.

% また思い出して欲しいのですが，分散分析で群間・群内平方和の比較をした結果，有意であると判断されても「どこが有意なのか」までは分からないので，事後検定が必要なのでした。Rはデフォルトで分散分析やTukeyの多重比較をやってくれる関数などを持っていますが，別々の関数になっていて，自分で1つ1つやっていくのは面倒なところもあります。この主な分析$\to$事後の検定という一連の流れを，自動的にやってくれる便利な関数があります。
I want you to remember this, but even if the comparison of between-group and within-group sum of squares in the analysis of variance is judged as significant, we do not know "where is significant", so a post hoc test is necessary. R has functions that perform the analysis of variance and Tukey's multiple comparison by default, but they are separate functions, and it can be a hassle to do them one by one by yourself. There is a handy function that automatically does this main analysis -> post hoc test sequence for you.

% 井関龍太さんの\texttt{anovakun}というのがそれで，ネットで一般に公開されています\footnote{\url{http://riseki.php.xdomain.jp/index.php?ANOVA君}}。CRANからダウンロードするパッケージのようにはなっていませんので，これを使うにはローカルに\texttt{anovakun}関数を書いたファイル(執筆時点，2021年11月現在で\verb|anovakun_486.txt|)をダウンロードし読み込むか，Rから直接インターネット経由でダウンロードして使うか，の2通りの方法のいずれかを使う必要があります。どちらにしても，Rの\texttt{source}関数をつかうもので，ダウンロードしたファイルを使う場合は\underline{プロジェクトフォルダに当該ファイルをダウンロードした上で}，読み込むコードを実行します(\texttt{code:\ref{code:23_03}})。
Ryuta Iseki's \texttt{anovakun} is available online for general public\footnote{\url{http://riseki.php.xdomain.jp/index.php?ANOVA君}}. It isn't set up like a package that you can download from CRAN, so to use it, you either need to download the file with the \texttt{anovakun} function written on it to your local machine (as of writing in November 2021, it's called \verb|anovakun_486.txt|), or download it directly from the internet via R. Regardless of which method you choose, you'll need to use the R \texttt{source} function, and if you're using the downloaded file, you'll need to first download it to your project folder, then execute the code to load it (\texttt{code:\ref{code:23_03}}).

\input{m22_anova_onR_escape_08}

% ネットから直接ダウンロードする場合は，インターネットにつながっている状態で\texttt{code:\ref{code:23_04}}を実行します\footnote{リンク先を転記するのは大変です。Googleで「ANOVA君」を検索し，井関先生のサイトに飛んでから，「下のアイコンをクリックしてファイルを保存してください。」とあるANOVA君ソースファイルのリンク先を右クリック，リンク先をコピーすれば確実です。ちなみにこのアドレスはリンクの長さを縮めるLink Shortnerサービスのひとつ，bit.lyを使っています。}。
When direct downloading from the internet, execute \texttt{code:\ref{code:23_04}} while connected to the internet\footnote{It's challenging to transcribe the link. Try to Google "ANOVA-kun" (ANOVA Mr.), go to Professor Iseki's website, then right-click the link where it says "Please click the icon below and save the file" to get the ANOVA-kun source file and copy the link. This address uses bit.ly, one of the Link Shortner services, to shorten the length of the link.}.


\input{m22_anova_onR_escape_09}

% これらのコードが実行されると，Rの環境にたくさんの関数が入っているはずです(図\ref{fig::22_01})。これらが入っていればOKです。
When these codes are executed, there should be many functions in the R environment (Figure \ref{fig::22_01}). If these are included, it's okay.
\input{m22_anova_onR_escape_10}
% それでは実際の使い方を見ていきましょう。
Let's take a look at how to actually use it.
% \section{一要因Between計画の実際}
Sure, there seems to be a mistake as the text appears to be LaTeX code, not Japanese. But, assuming that you want to translate "\section{一要因Between計画の実際}" into English, it would be "\section{Reality of One-Factor Between-Group Design}" depending on the context.
% ではまず一要因Between計画の話から。例として第\ref{m21_anova}講のデータを使います。一応再掲しておきましょう(表\ref{table::23_01})。
Let's start with the story of the "Between" plan as one factor. We will use the data from lecture \ref{m21_anova} as an example. Let's post it again for the time being (Table \ref{table::23_01}).
\input{m22_anova_onR_escape_11}
% まずはこれを線形モデルとして分析するところからです。\texttt{code:\ref{code:23_05}}のようにデータフレームを組み上げます。
We start by analyzing this as a linear model. We build a dataframe like in \texttt{code:\ref{code:23_05}}.

\input{m22_anova_onR_escape_12}

% \paragraph{コード解説}
\paragraph{Code Explanation}
\input{m22_anova_onR_escape_13}

% \subsection{線形モデルとして分析}
\subsection{Analysis as a Linear Model}
% 線形モデルとしての分析は，前期でやったように\texttt{lm}関数を使います(\texttt{code:\ref{code:23_06}})。
Analysis as a linear model uses the \texttt{lm} function as done in the previous term (\texttt{code:\ref{code:23_06}}).

\input{m22_anova_onR_escape_14}

% 1行目が\texttt{lm}関数を使っていて，結果を\texttt{result.lm1}オブジェクトに入れているところです。従属変数と独立変数をチルダ\verb|~|で繋げているところ，思い出してください。2行目はこの結果の出力になります。結果は\texttt{Rの出力\ref{RS:out23_03}}のように示されているはずです。
The first line is using the \texttt{lm} function and puts the result in the \texttt{result.lm1} object. Remember the part where the dependent variable and the independent variable are connected with a tilde \verb|~|. The second line is the output of this result. The result should be shown like \texttt{R output\ref{RS:out23_03}}.
\input{m22_anova_onR_escape_15}
% これを見ると，切片\texttt{(Intercept)}の推定値が5.5，次に2つの説明変数の係数が書いてあります。文字が連なっているのでわかりにくいかもしれませんが，それぞれ\texttt{condition}と\texttt{exp1}の関係，\texttt{condition}と\texttt{exp2}の関係を表しています。図\ref{fig::23_02}を見ると結果の意味は分かり易いと思います。
When you see this, the estimated value of the intercept is 5.5, followed by the coefficients of the two explanatory variables. It may be hard to understand because the characters are connected, but they represent the relationships between \texttt{condition} and \texttt{exp1}, and \texttt{condition} and \texttt{exp2}, respectively. If you look at Figure \ref{fig::23_02}, you should easily understand the meaning of the results.
\input{m22_anova_onR_escape_16}

% 第\ref{m21_anova}講では，線形モデルを
In lecture \ref{m21_anova}, we discuss linear models.
\[ X_{ij} = \beta_0 + \beta_j + \varepsilon_{ij} \]
としていました。ここで$\beta_0$は全体平均でした。全体平均を基本として，各水準に対する効果$\beta_j$がどうなったか，というモデル化でしたが，ここでは
\[ X_{ij} = \bar{X}_1 + \beta_j + \varepsilon_{ij} \]
% という形になっています(図\ref{fig::23_02})。本質的に違うものではありません。統制群に比べて実験群1の平均値は$+0.5$，実験群2は$-1.25$の平均値の違いがあるのだな，と思っていただければ結構です。その後ろにStd.Errorや$t$値が出ていますが，これらは推定された回帰係数の標準誤差および，係数が$0$であるという帰無仮説を棄却できるかどうかの検定結果が出ています\footnote{各群の母平均の推定値でもありますが，各群について一変数の$t$検定をしているのではなく，これら線形モデル全体として(他の説明変数に条件づけられた分布の中で)標準誤差を計算します。詳しくは\textcite{Haebara200206}のp.118を参照してください。}。
It is as shown in Figure \ref{fig::23_02}. Essentially, there is no significant difference. The average value of the experimental group 1 is $+0.5$ and that of the experimental group 2 is $-1.25$ compared to the controlled group. It's fine if you think that there is such a difference in the average values. Behind this, Std.Error and the $t$ value are shown, which indicates the standard error of the estimated regression coefficient and the test result of whether the null hypothesis that the coefficient is $0$ can be rejected or not\footnote{Although they are also estimates of the population mean for each group, they are not doing a one-variable $t$ test for each group, but are calculating the standard error for the linear model as a whole (in the distribution conditioned by other explanatory variables). For more details, please refer to p.118 of \textcite{Haebara200206}.}.


% 線形モデルですから，モデルの適合度(\texttt{Multiple R-squared, Adjusted R-squared})なども出ていますが，それよりも最後の一行，この線形モデル全体の検定結果が出ています。これは要因計画のもう1つの切り口，分散分析による結果と同じものです。では分散分析の視点からこの分析結果を見てみましょう。
This is a linear model, so the model's goodness of fit (Multiple R-squared, Adjusted R-squared) etc. are outputted, but more than that, the last line shows the result of testing the entire linear model. This is the same result as the variance analysis, which is another perspective on the factorial design. Now, let's look at this analysis result from the perspective of variance analysis.

% \subsection{帰無仮説検定として分析}
\subsection{Analysis as a Null Hypothesis Test}
% 要因計画の帰無仮説検定版，つまり分散分析の結果ですが，線形モデルと同じなので今回得られた出力をすこし変えるだけで対応できます。最も単純には，先ほどの線形モデルの結果を\texttt{anova}関数に入れると，分散分析表が次のように示されます(\texttt{code:\ref{code:23_07}},\texttt{Rの出力\ref{RS:out23_04}})。
This is the null hypothesis testing version of the factorial design, in other words, the result of variance analysis. As it is the same as the linear model, we can deal with the obtained output with a slight modification. Most simply, by inputting the result of the linear model into the \texttt{anova} function, the variance analysis table is shown as follows (\texttt{code:\ref{code:23_07}}, \texttt{R output\ref{RS:out23_04}}).


\input{m22_anova_onR_escape_17}


\input{m22_anova_onR_escape_18}
% ここから，$F(2,9)=2.4894,p=0.1379$で統計的に有意な差はみられなかった，と判断できます。これらの数字，どこをどう読み取ったのか確認してください。
From here, with $F(2,9)=2.4894, p=0.1379$, it was determined that there was no statistically significant difference. Please check these numbers and see how and where they were read.

% これだけですから，\texttt{lm}関数だけでいいじゃないかと考える人もいるかもしれません。基本的には問題ないのですが，分散分析は「群のサンプルサイズが違う場合」や，「分散の計算」においていくつかの計算バリエーションがあり，Rデフォルトの\texttt{lm}関数では十分対応できなくなるシーンが出てきます。
There might be people who think that the \texttt{lm} function is sufficient since it's just this. Essentially, it's not a problem, but there are several calculation variants in the analysis of variance when "the sample size of the groups is different" or in "the calculation of variance", and there will come a scene where the R default \texttt{lm} function is no longer sufficiently compatible.

% そこで先ほど導入した，分散分析専用関数\texttt{anovakun}を使います。実際の使い方は次のようになります(\texttt{code:\ref{code:23_08}})。
We will use the covariance analysis function \texttt{anovakun} introduced earlier. The actual usage is as follows (\texttt{code:\ref{code:23_08}}).
\input{m22_anova_onR_escape_19}

% まず，\texttt{anovakun}には分散分析に必要なデータだけを与える必要があります。今回のデータフレームにあるid列は入りませんので，\texttt{Example1[,2:3]}としてデータの2列目と3列目だけ(2列目から3列目まで)に限定しています\footnote{大括弧$\left[, \right]$は，データフレームの行および列を指定するのに使います。 一行目だけを指定したい場合は\texttt{Example1[1,]}，一列目だけ指定したい場合は\texttt{Example1[,1]}，1行1列目の要素だけアクセスしたい場合は\texttt{Example1[1,1]}のようにします。}。次に書いてある\texttt{As}は，\texttt{anovakun}独特の記法です。ここでの\texttt{s}は被験者subjectを意味しており，これの左側に群間要因を，右側に\keytermL{群内要因}{ぐんないよういん}を書きます。今回は一要因の群間デザインですので，左側に1つ要因Aをおく，という書き方になっています。これを\texttt{sA}のようにすると一要因群内デザイン，\texttt{AsB}とすると，要因Aは群間で要因Bは群内の混合計画を表すことになります。最後に，今回の一要因Aの水準数を入力すれば，\texttt{anovakun}関数はすべての分析を実行してくれます。オプションとして，効果量$\varepsilon^2$を表示するスイッチをオン(\texttt{eps=T})とすると，効果量も出してくれます(\texttt{Rの出力\ref{RS:out23_05}})。
First, you need to provide \texttt{anovakun} with only the data necessary for variance analysis. The id column in this data frame does not enter, so it is limited only to the second and third columns of the data as \texttt{Example1[,2:3]}\footnote{The brackets $\left[, \right]$ are used to specify the rows and columns of the data frame. If you want to specify only the first row, use \texttt{Example1[1,]}, if you want to specify only the first column, use \texttt{Example1[,1]}, and if you want to access only the element of the first row and first column, use \texttt{Example1[1,1]}.}. The \texttt{As} written next is a unique notation of \texttt{anovakun}. The \texttt{s} here means the subject, and the intergroup factors are written on the left side and the intra-group factors on the right side. This time, it is a one-factor intergroup design, so the way of writing is to put one factor A on the left. If you make this \texttt{sA}, it will be a one-factor intra-group design, and if you make it \texttt{AsB}, it will represent a mixed plan where factor A is intergroup and factor B is intragroup. Finally, if you enter the number of levels of the one-factor A, the \texttt{anovakun} function will execute all analysis. As an option, if you turn on the switch to display the effect size $\varepsilon^2$ (\texttt{eps=T}), it will also give you the effect size (\texttt{R output \ref{RS:out23_05}}).

\input{m22_anova_onR_escape_20}
% 各群の平均値，標準偏差および，分散分析表が表示されていますね。これを見て，どことどこの数字を参照すれば良いか等，確認しておいてください。
The averages, standard deviations of each group, and the analysis of variance table are displayed. Please check this out and see which numbers to refer to and so on.

% これだけだと，とくに\texttt{anovakun}が便利そうに思えないかもしれません。が，以下さまざまな要因計画に対応しているので，徐々にその良さがわかってくると思います。
You might not find \texttt{anovakun} particularly useful with just this. However, since it is compatible with various factor plans as shown below, I believe you will gradually come to appreciate its benefits.
% \section{二要因Between計画の実際}
Sure, but I haven't noticed any Japanese text in your statement. Could you please provide the Japanese text you want me to translate?
% それでは続いて，二要因の計画にいきましょう。データを再掲しておきましょう(表\ref{tbl::23_02})。
Let's proceed to the plan with two factors next. Let's reprint the data (Table \ref{tbl::23_02}).
% latex table generated in R 4.0.2 by xtable 1.8-4 package
% Fri Sep 11 14:11:29 2020
\input{m22_anova_onR_escape_21}
% これをデータフレーム型にします。温度，メーカーは名義尺度水準なので要因型にします(\texttt{code:\ref{code:23_09}})。
This will be made into a data frame type. Since the temperature and manufacturer are nominal scale levels, these will be converted into factor type (code: \ref{code:23_09}).
\input{m22_anova_onR_escape_22}
% \subsection{線形モデルとして分析}
\subsection{Analysis as a Linear Model}
% これでデータはできたので，分析にかけるだけですね。二要因以上に計画が複雑になる場合は，交互作用を考えなければならないことに注意が必要です。線形モデルで表現するには，次のようにします(\texttt{code:\ref{code:23_10}})。
Now that the data is ready, all that's left is to analyze it. Please note that if the plan becomes complicated with more than two factors, it's necessary to consider the interactions. To express it in a linear model, do as follows (\texttt{code:\ref{code:23_10}}).
\input{m22_anova_onR_escape_23}
% ここで，説明変数が$\ast$でつなげられています。これで主効果と交互作用の両方をモデルの中に入れたことになります。結果は\texttt{Rの出力\ref{RS:out23_06}}のように表示されるはずです。
Here, the explanatory variables are connected by an asterisk. This includes both the main effects and interactions in the model. The results should be displayed like \texttt{R output\ref{RS:out23_06}}.

\input{m22_anova_onR_escape_24}
% ここに示されているのは，温度\texttt{temp}変数の第一水準(Hot)かつメーカー\texttt{maker}変数の第一水準(銘柄A)を基準にした時に，\texttt{tempCold}つまりメーカーは同じ(銘柄A)で温度だけ違う群は平均値が$-3.0$，次にメーカーをBで\texttt{makerB}温度はHotの群と比べるとその平均値は$-5.0$，両方変えると$+7.0$になる，ということです。それぞれ標準誤差や$t$値が出ていますが，これは傾きが0である，という帰無仮説を棄却しているだけであって，効果の大きさについてはわかりません。
What is shown here is that when the temperature variable \texttt{temp} level 1 (Hot) and the manufacturer variable \texttt{maker} level 1 (Brand A) are taken as a reference, the \texttt{tempCold} group, which only differs in temperature, but has the same manufacturer (Brand A), has an average value of $-3.0$. When compared with the group where the manufacturer is B and the temperature is Hot (\texttt{makerB}), the average value is $-5.0$. Changing both leads to an increase of $+7.0$. This is the implication. Although standard errors and t-values are given, they simply reject the null hypothesis that the slope is zero, and do not provide an understanding of the effect size.

% また，最後に$F$統計量が出ています。自由度が$(3,8)$の時に$p=0.004641$ですが，これは線形モデル全体としてみた時に横一線の帰無仮説モデルとは違うね，というだけであって，具体的にどこがどう違うのか，というのがわかりにくくなっています。
Also, finally, the F statistic is mentioned. It is $p=0.004641$ when the degrees of freedom are (3,8), but this only means that when looking at the entire linear model, it differs from the null hypothesis model of a single horizontal line. However, it becomes difficult to understand specifically what and how it differs.

% そこでそれぞれの水準について検証できる，分散分析モデルで分析してみます。
Let's try to analyze it with a variance analysis model that can verify each level.
% \subsection{帰無仮説検定として分析}
\subsection{Analysis as a Null Hypothesis Test}
% 要因計画の帰無仮説検定版，でやってみましょう。先ほどと同じように，Rがデフォルトで持っている関数\texttt{anova}に線形モデルの結果を入れてみます(\texttt{code:\ref{code:23_11}})。
Let's try the null hypothesis testing version of the factorial plan. Just as before, try inserting the results of the linear model into the \texttt{anova} function that R has by default (\texttt{code:\ref{code:23_11}}).
\input{m22_anova_onR_escape_25}

% 結果は\texttt{Rの出力\ref{RS:out23_07}}のようになります。
The result will come out as shown in the \texttt{R output\ref{RS:out23_07}}.
\input{m22_anova_onR_escape_26}
% 分散分析表だけでなく，ご丁寧に有意かどうかわかりやすいような記号(\verb|.|や\verb|*|など)をつけてくれています。最後の一行には\texttt{Signif. codes}とあって，米印が1つなら5\%，2つなら1\%，3つなら0.1\%水準で有意である，という読み方が書いてあります\footnote{おそらくこれが諸悪の根源で，なんとなく星の数が多い方が「良い結果」のような印象を持ちますし，分析したらとりあえず星を探す，という仕草が生まれるようになっていますよね。帰無仮説検定はあくまでもモデル比較の判定であって，判定基準が厳しいほど「良い」とか，基準をクリアできなかったから「惜しい(残念)」という評価を伴うものではないはずなのに，です。有意差がなかったら，そういう結果が得られた，というだけですし，星の数が小さくてもえらいとかすごい，ということにはならない，ということを再確認してください。「心理学者は星を探して喜んでるんだ」というようなジョークの的にならないように，適切な分析的素養を身に付けましょう。}。これを見ると，\texttt{maker}水準は5\%水準で有意にはならず\footnote{世の中には「有意傾向」という変な言葉があります。今回の$p$値は6\%でしたが，これは5\%水準ではアウトですよね。こういった5\%をちょっとだけ上回っちゃった，惜しい！という時に，「有意になりそうな傾向があった」略して「有意傾向があった」という表現がされることがあるのです。あるいは「傾向差があった」という言葉もまことしやかに使われていたりします。が，これらは心理学独自の間違った表現であり，決して使うべきではありません。なぜなら，5\%だろうが6\%だろうが，任意に決めた判定基準であって，$p$ 値の大小関係は現実の効果の大きさとは関係がないからです。}，交互作用項(\texttt{temp:maker})は有意になっていることがわかります。
Not only the analysis of variance table, but also carefully added symbols (such as \verb|. |and \verb|*|) that make it easy to understand whether it is significant. The last line says \texttt{Signif. codes}, and it is written how to read that if there is one asterisk, it is significant at the 5% level, if there are two, it is significant at the 1% level, and if there are three, it is significant at the 0.1% level.\footnote{Probably this is the root of all evils, it seems that you get the impression that the more stars, the "better the result", and the gesture of looking for stars as soon as you analyze it is born. Null hypothesis testing is just a model comparison decision, even if the decision criterion is strict, it is not supposed to come with an evaluation that "good" or "a pity (a pity)" because it could not clear the criterion. Even if there is no significant difference, it is just a result that has been obtained, and even if the number of stars is small, it is not a brilliant or amazing thing, please reconfirm. Let's acquire appropriate analytical cultivation so that we don't become a joke like "Psychologists are looking for stars and are happy"} When you look at this, the \texttt{maker} level doesn't become significant at the 5% level\footnote{There is a strange word in the world called "significant tendency". This time the $p$ value was 6%, but this is out at the 5% level, isn't it? When it just exceeds 5% a little bit, it's a pity! There are occasions when expressions such as "There was a tendency to be significant" abbreviated as "There was a significant tendency" are made. Or there are words like "There was a trend difference" which are used credibly. However, these are incorrect expressions unique to psychology and should never be used. Because, whether it is 5% or 6%, it is an arbitrarily determined criterion, and the relationship between the size of the $p$ value has nothing to do with the size of the real effect.}, however the interactive term (\texttt{temp:maker}) is found to be significant.

% ただ，このままだと「交互作用は有意だけど，どこがどう違うのかわからない」という状況ですね。4つの母平均が同じである($\mu_{11}=\mu_{12}=\mu_{21}=\mu_{22}$)という帰無仮説が棄却されただけで，どこがどう違うのかについては事後の検定を追加しなければならないからです。
However, as it stands, the situation is, "The interaction is significant, but I don't know where and how it differs." This is because the null hypothesis that the four population means are all equal ($\mu_{11}=\mu_{12}=\mu_{21}=\mu_{22}$) has only been rejected. In order to understand how and where it differs, additional post-hoc testing is required.

% ではこれを\texttt{anovakun}でやってみましょう。彼(?)なら一気にすべての答えを出してくれるのです(\texttt{code:\ref{code:23_12}})。
"Let's try this with \texttt{anovakun}. If he(?) does, he will give all the answers at once (\texttt{code:\ref{code:23_12}})."
\input{m22_anova_onR_escape_27}
% 先ほどと同様，\texttt{anovakun}にはid列やnum列は必要ありませんので，3行目から5行目まで，要因A,Bと従属変数だけを与えます。要因計画は間$\times$間ですので，被験者\texttt{s}の左側にA,Bとしています。2つの要因はそれぞれ2水準ですので，水準数を2つ書いています。最後の\texttt{eps=T}は，効果量である$\varepsilon^2$を表示するスイッチをオン(TRUE)，ということです。
Just like before, \texttt{anovakun} doesn't require id and num columns, so from the 3rd to 5th line, we just provide the factors A, B and the dependent variable. The factorial design is within $\times$ within, so to the left of the subject \texttt{s}, it is expressed as A, B. Since the two factors have two levels each, the level count is listed as 2. The final \texttt{eps=T} is switching on(TRUE) the display of the effect size $\varepsilon^2$.

% 結果は\texttt{Rの出力\ref{RS:out23_08}}のように表示されます。
The result is displayed as shown in R's output \ref{RS:out23_08}.
\input{m22_anova_onR_escape_28}

% 最初に，水準ごとの平均値と標準偏差が表示され，続いて分散分析表が示されています。
First, the mean and standard deviation for each level are displayed, followed by a presentation of the variance analysis table.
% これを見ると，要因A(温度)の主効果はなく($F(1,8)=0.500,p=0.4996$)\footnote{米印があるべきところに$ns$とありますが，これはnot significant，つまり有意ではないという意味です。}，ついで要因Bの効果も5\%水準では認められない(10\%水準で有意，その記号として$+$が使われています)，交互作用は5\%水準で有意という結果になりました。ここまでは当然，先ほどと同じですね。効果量$\varepsilon^2$の列を見ると，交互作用は全体の62\%の分散を説明したことになりますから，これはかなり大きな効果だったようだ，ということがわかります。
Looking at this, there is no main effect for factor A (temperature) ($F(1,8)=0.500,p=0.4996$\footnote{'ns' is where it should be marked with a '米', which means 'not significant', in other words, it is not significant.}), and then the effect of factor B is also not recognized at the 5% level (significant at the 10% level, the symbol '$+$' is used for this), the interaction is significant at the 5% level. This outcome was expected, same as the earlier analysis. When you look at the column of effect sizes $\varepsilon^2$, the interaction accounted for 62% of the total variance, so it seems that it had a quite large effect.

% 続いてPOST ANALYSISとして事後の検定が表示されています。SIMPLE EFFECTS for "A x B" INTERACTIONとあるのは，「AとBの交互作用についての単純効果」という意味で(直訳！)，各水準に限定した時に他の水準の効果があったかどうかをみています。たとえば最初の行，A at b1とありますが，これは要因B(銘柄)の第一水準における要因A(温度)の効果をみていることになります。以下同様ですが，これらは分散分析の表をより細かくしてチェックしているようなものです。これを見ると，冷たい時の銘柄には差がない(B at a2)ようですが，それ以外の差は統計的に有意，と言えそうです。
The text translates as:

Next, a post-hoc test is displayed as a POST ANALYSIS. "SIMPLE EFFECTS for "A x B" INTERACTION" means "simple effects for the interaction of A and B" (literal translation), and examines whether there were effects of other levels when limited to each level. For example, the first line says "A at b1", which means we are looking at the effects of factor A (temperature) at the first level of factor B (brand). The lines below do the same, and they seem to check the table of variance analysis in more detail. Looking at this, it seems that there is no difference in brand when it's cold (B at a2), but it seems statistically significant to say that there are other differences.

% これらの結果を見やすくするために，図\ref{fig::23_03}に対応するところに線を引いてみました。結果と見比べて，どこに差があったか確認しておいてください。
To make these results easier to view, I have drawn lines at the corresponding places in Figure \ref{fig::23_03}. Please check against the results to confirm where there were differences.
\input{m22_anova_onR_escape_29}

% また，これらの結果を報告する時は，次のように書くと良いでしょう。
Moreover, when reporting these results, it would be good to write it as follows.
\input{m22_anova_onR_escape_30}
% \section{一要因Within計画の実際}
You didn't provide any Japanese text. Please provide the text you want to be translated.
% 最後にWithin計画の例をやってみましょう。仮想データとして次のような表\ref{tbl::23_03}を使います。
Let's try an example of the Within plan at the end. We will use the following table \ref{tbl::23_03} as the virtual data.
\input{m22_anova_onR_escape_31}

% これをデータフレーム型にします(\texttt{code:\ref{code:23_13}})。
Turn this into a dataframe type (code:\ref{code:23_13}).
\input{m22_anova_onR_escape_32}

% 線形モデルとして表現するには，少し複雑な操作が必要ですので\footnote{具体的には，一人ひとりに回帰分析をしつつ，個々人の切片＝個人差がより上位の確率分布にしたがっている，という階層的線形モデルという形になり，次回以降で取り上げる確率モデルの導入をしないといけないのです。Rで実行する場合も別途パッケージを導入する必要があり，とても今回の残り時間で扱い切れる分量ではないので，割愛させていただきました。興味がある人は二年次のデータ解析応用でお会いしましょう。}，今回は分散分析的アプローチのみ紹介したいと思います。
To represent this as a linear model, a bit of complicated operation is necessary\footnote{Specifically, it takes the form of a hierarchical linear model where we perform regression analysis on each individual while the individual intercepts, i.e., individual differences, follow a higher level probability distribution. This requires the introduction of a probability model, which we will cover after this. In the case of execution in R, separate packages need to be installed as well, and it's too much to handle in the remaining time for this session, so I had to skip it. If you're interested, let's meet in the advanced data analysis in the second year.}, so for now I only want to introduce an approach similar to analysis of variance.
% \subsection{帰無仮説検定として分析}
\subsection{Analysis as Null Hypothesis Testing}
% 群内モデルの\texttt{anovakun}での入力は次のようになります(\texttt{code:\ref{code:23_14}})。
The input to the within-group model's \texttt{anovakun} is as follows (\texttt{code:\ref{code:23_14}}).
\input{m22_anova_onR_escape_33}
% データセットや変数の指定，効果量オプションの指定はとくに追記することはありません。デザインの指定が\texttt{"sA"}と\texttt{s}の右側に来ているところがポイントです。これを実行すると，\texttt{Rの出力\ref{RS:out23_09}}のように結果が示されます。
There is nothing in particular to add regarding the specification of datasets, variables, or effect size options. The point here is that the design specification comes to the right of \texttt{"sA"} and \texttt{s}. When you run this, the results are shown as in \texttt{Output from R \ref{RS:out23_09}}.

% まず群ごとの記述統計量が計算されています。その次に\verb|<< SPHERICITY INDICES >>|とありますね。これは\keyterm{球面性の検定}{きゅうめんせいのけんてい}{sphericity test}と訳されます。$t$検定の時にWelchの補正をしたのを思い出して欲しいのですが，分散分析の時も基本的に母分散が同じである，という仮定があります。この仮定が満たされない場合は補正をしなければなりません。とくにWithin計画の場合は，全部の分散(共分散)が等しいという仮定を置くのではなく，少なくとも全方向に等しく広がっている空間であるはずだ，という仮定をおいています\footnote{Within計画の時にややこしいのは，モデルでいうと個々人に線形モデルを仮定して，個人差＝切片が異なるような階層的なモデルになっているということです。個々人の分散が同じ，というのはなかなか厳しい仮定だと思われますが，水準同士の相関関係(共分散)も考慮に入れて，分散共分散行列(対角要素に分散，非対角要素に共分散が入っている正方行列)が均質である，という仮定ぐらいはせめて保持したいわけです。水準数が増えるとこの相関関係を表現するする世界は多次元空間に広がっていくので，究極的には丸くなる，球面性の検定といわれる所以です。 }。
First, the descriptive statistics for each group are calculated. Then, you will notice there is \verb|<< SPHERICITY INDICES >>|. This is translated as a \keyterm{sphericity test}. I would like you to remember the time we did the Welch's correction in the $t$-test, as it is fundamentally assumed that the population variances are the same, even in the analysis of variance. When this assumption is not met, corrections must be made. Especially in the case of within-subjects design, the assumption is not that all variances (covariances) are equal, but that the space should spread equally in all directions\footnote{What's tricky about within-subjects design is that it assumes a linear model for each individual in the model, with individual differences being different intercepts, making it a hierarchical model. While it seems stringent to assume each individual's variance is the same, taking into account the correlations (covariances) between the levels, we at least want to maintain the assumption that the variance-covariance matrix (a square matrix with variances in the diagonal elements and covariances in the non-diagonal elements) is homogeneous. As the number of levels increases, this correlation is represented in a multi-dimensional space, ultimately becoming round, hence the term sphericity test. }.
\input{m22_anova_onR_escape_34}
% これが満たされない場合は，Greenhouse-Geisserの補正やHuynh-Feldt-Lecoutreなどの補正をしなければならないが，どうでしょうね？ というのを確認するのがこの箇所です(\texttt{Rの出力\ref{RS:out23_10}})。今回は$p$が1.000で$ns$，つまりnot significant=有意に歪んでいるとは言えない，ということなので補正は不要です。もしここが有意である，つまり丸いという状況から異なっている現状があると思われるのであれば，自由度などの補正が必要で，その時\texttt{anovakun}はオプションで補正を指定できます。ともかく今回は，このまま先に進めそうです\footnote{まったく！検定の多重性が問題だとかなんとか言いながら，そもそも前提が満たされているかどうかの検定は挟まないといけないし，ここでは有意差がでると補正，出ないとOKってなんかいつもとは違う判断をしないといけない。分散分析とか帰無仮説検定というのは，とても厳密な手続きが必要なのですが，こんな感じであちこちに補正，修正などが入ってきて，正直嫌になりますよね。私は嫌なのでそういう悩みのないベイズ統計的アプローチの方がいいと思うんですが，みんなよくこんなのまじめにやってきたなあと思いますよ。}。
In case this is not fulfilled, corrections must be made, such as Greenhouse-Geisser or Huynh-Feldt-Lecoutre, but what do you think? This is the part to check that (\texttt{R output \ref{RS:out23_10}}). This time, $p$ is 1.000 and $ns$, in other words, it can't be said that it is significantly distorted, so correction is not necessary. If this is significant, that is, if you think there is a current situation that deviates from a round one, corrections of degrees of freedom, etc. are necessary, and at that time \texttt{anovakun} can specify the correction as an option. Anyway, this time, it seems that we can proceed as it is\footnote{Absolutely! While saying that the multiplicity of testing is a problem, we have to insert a test whether the premise is met, and here we have to make a different decision than usual, if there is a significant difference, correction, if there is no difference, it's okay. Variance analysis and the null hypothesis test require very strict procedures, but various corrections and modifications come in like this, honestly, it's disgusting. I don't like it so I think it's better to have a Bayesian statistical approach without such concerns, but I wonder everyone has been seriously doing this.}.

\input{m22_anova_onR_escape_35}
% その先にはやっと分散分析表が出てきました。これを見ると\texttt{Source}列の\texttt{s}とあるところ，これが個人差です。ここについては検定の対象外なので，$F$値などの計算は行われていません。その下の\texttt{A}からが要因の効果で，\texttt{s x A}とあるところ，つまりAに伴って生じる個人差，のところが比較対象となる誤差のところです。この$F$値が16.00，$p=0.0039$で統計的に有意(ちなみに効果量$\varepsilon^2=0.3896$)なので，水準間のどこかには差があるということがわかります。
Finally, we have the ANOVA (Analysis of Variance) table. Looking at this, in the 'Source' column, there is 's', which represents individual differences. This is not subject to the test, so no calculations such as an F-value are done for this. Just below that, starting from 'A', is the effect of the factor, and where it says 's x A'; that is, the individual differences that arise with A, is where the comparable error comes from. This F-value is 16.00 and p=0.0039 is statistically significant (by the way, the effect size ε²=0.3896), so we can understand that there is a difference somewhere between the levels.

% 続く\verb|<< POST ANALYSES >>|のところに，Shefferの方法を使った事後の検定があります。これによると，a1-a2のペアは$a1 > a2$の関係にあって有意で，そのほかのペアは有意ではない，ということがわかります。
In the following \verb|<< POST ANALYSES >>| section, you will find the post-test using Sheffer's method. According to this, the pair of a1-a2 is significant with the relation of $a1 > a2$, and the other pairs are not significant.

% 結果としては，たとえば次のように報告するといいでしょう。
As a result, you could report it like this, for example.
\input{m22_anova_onR_escape_36}

% 以上で解説を終わります。
I will conclude the explanation at this point.
% 分散分析を面倒で煩雑な作業だ，と思った人も少なくないと思います。事実そうなのですが，だからと言って適当に実行してしまうと，誤用・誤解など問題のある研究実践になる可能性がありますので，細部まで細かく配慮しながら，どこにどういう意味があって，どのような読み取り方をするべきなのかをしっかりと理解しておいてください。
I believe there are not a few people who have found variance analysis to be a troublesome and complicated task. While this is true, if you proceed carelessly, it may lead to problematic research practices such as misuse or misunderstandings. Therefore, please understand thoroughly the significance and interpretation in each detail, providing careful consideration.

% このほか，たとえば三要因計画になるとか，混合計画になるなどすると，それぞれ分散分析表や事後の分析の読み取り方などでアウトプットがどんどん長くなります。が，そこで注意を切らすのではなく，落ち着いて眺めてみてください。基本的には同じような前提，作業工程を，繰り返し繰り返し適用していくだけなのです。複雑で難解なのではなく，単純で煩雑なので嫌気が差してしまうだけです。ここでしっかりと腹を据えて読み取れるようになれば，分散分析は意外と素直なモデルに見えるかもしれません。
In addition, for example, as this changes to a three-factor design or mixed design, the output becomes longer and longer, due to different ways of reading analysis of variance table and post analysis etc. But don't lose patience at that point, just calmly observe. Basically, the same assumptions and tasks are applied over and over again. It's not complicated or difficult, it's just simple and tedious so you may get bored. However, if you can read thoroughly here, the variance analysis may look surprisingly straightforward.

% それでも帰無仮説検定という実践的判断基準を実践するので，ある種の断定(有意差があった，なかった)が以後の解釈を歪めてしまいかねないという可能性は否定できませんし，確率的判定をしている以上は検定の多重性の問題からは逃れることができません。これを乗り越えるためには，もう少し確率的なモデルのことを詳しく理解する必要がありますし，モーメント法(平均値や不偏分散で母数を推定する)ではないほかの方法で考える必要もあります。続く講義ではこれらについて解説していきます\footnote{分散分析はオワコンだ，と思われて久しいのですが，今でも学術論文には帰無仮説検定が実践されているので，みなさんには古典を読むためのリテラシーとして教えています。}。
Nevertheless, by practicing a practical judgment criterion called null hypothesis verification, we cannot deny the possibility that some kind of conclusion (there was a significant difference, there was not) could distort subsequent interpretations. Also, as we make probabilistic judgments, we cannot escape the issue of multiplicity of testing. To overcome this, we need to understand probabilistic models in more detail, and we need to think in ways other than the method of moments (estimating parameters with mean value and unbiased variance). In the following lectures, we will explain these. Please note that while it has been thought that variance analysis is obsolete for a long time, null hypothesis tests are still being practiced in academic papers. Therefore, we are teaching you as literacy to read classics.
% \section{課題}
\section{Task}
% \paragraph{課題1 線形モデルとしての分析}
\paragraph{Task 1 Analysis as a Linear Model}
% ある学校の国語の授業で，ある教師が4つのクラスそれぞれに対して個別のテキストを使って授業を行い，同一のテストを行うことでテキストの効果があるかどうかを検証したとします。
Suppose in a language class at a certain school, a teacher individually conducted classes using different texts for each of the four classes and, by implementing the same test, verified whether the texts had an effect or not.
% 各クラスには10人の生徒がいて，それぞれのテスト結果が次の通りです(表\ref{L23::exe1})。次の各指示に従い，その結果について回答してください。なお，このデータセットは\texttt{chapter22exe1.csv}ファイルに入っています。
There are 10 students in each class, and their test results are as follows (Table \ref{L23::exe1}). Please answer about the results following each instruction. Note that this data set is contained in the \texttt{chapter22exe1.csv} file.
\input{m22_anova_onR_escape_37}

\input{m22_anova_onR_escape_38}

% \paragraph{課題2 Betweenデザインの分散分析}
Sure, the translation to English is:

\paragraph{Assignment 2 Between-Designs Analysis of Variance}
% 別の学校では，テキストをA,B,Cの3種類とし，教員$\aleph,\nabla$の2人がそれぞれのテキストを使って教えることにしました。1クラス12人，一学年6クラスを使っての実験です。
In another school, the texts were divided into three types, A, B, and C, and two teachers, Aleph and Nabla, each decided to use these texts for teaching. The experiment is conducted with 12 students per class, using 6 classes per grade.

% 授業後のテストの点数が表\ref{L23::exe2}のようになっています。次の各指示に従い，その結果について回答してください。なお，このデータセットは\texttt{chapter22exe2.csv}ファイルに入っています。
The test scores after class are as shown in Table \ref{L23::exe2}. Please follow each of the following instructions and answer about the results. This data set is in the \texttt{chapter22exe2.csv} file.
\input{m22_anova_onR_escape_39}

\input{m22_anova_onR_escape_40}

% \paragraph{課題3 Withinデザインの分散分析}
\paragraph{Task 3 Analysis of variance in Within Design}
% さらに別の学校では，テキスト・教員は一種類・一人なのですが，4つの時期において成績が変化するかどうかを追跡調査しました。
Furthermore, at another school, although there is only one type of textbook and one teacher, a tracking survey was conducted to see whether grades change across four periods.
% 各時期のテストの点数が表\ref{L23::exe3}のようになっています。次の各指示に従い，その結果について回答してください。なお，このデータセットは\texttt{chapter22exe3.csv}ファイルに入っています。
The test scores for each period are as shown in Table \ref{L23::exe3}. Please follow the instructions for each and answer about the results. Also, this data set is stored in the \texttt{chapter22exe3.csv} file.

\input{m22_anova_onR_escape_41}

\input{m22_anova_onR_escape_42}

\end{document}
