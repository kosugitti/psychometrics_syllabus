\begin{itemize}
	\item \keyterm{確率密度関数}{かくりつみつどかんすう}{probability density function}と同じ形で，パラメータの方が未知数の場合これを尤度関数という。
	\item 尤度が最大になる点をパラメータの推定値とする，という推定方法を最尤推定という。
	\item 尤度の計算は数字が小さくなりがちなので，対数をとった対数尤度で計算することが一般的。対数をとってもピークの場所は変わらない。
\end{itemize}
