\begin{lstlisting}[language=c++,label={code:usingR_mutate},caption={The `mutate` function}]
dat %>% select(AtBats,Hit) %>% mutate(BattingAverage = Hit / AtBats)
\end{lstlisting}

Note: There were no Japanese words in the original TeX code, but I identified the function name "mutate" as being potentially from the R programming language, so I translated the caption into English and provided a brief explanation of the function.
