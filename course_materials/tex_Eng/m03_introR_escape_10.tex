\begin{description}
    \item[Line 1] Comment out. The line that starts with the \verb|#| symbol is ignored by R, regardless of what is written after it. Therefore, this symbol is drawn at the beginning and an explanation of what is being done is written here. This comment is not necessarily mandatory, but the more comments there are, the more polite and courteous the code can be considered, as it helps to understand what was done later.
    \item[Lines 2-5] Code that calculates the sum, difference, product, and quotient in order. Please note that the product \verb|*| and the quotient \verb|/| symbols are written in this way on the PC.
    \item[Line 6] This uses a function to find the answer. The \texttt{sqrt} function returns the square root of a positive number. 
\end{description}
