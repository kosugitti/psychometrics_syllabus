\begin{description}
    \item[Line 1] Add a function to draw smooth lines to the \texttt{g2} object and assign it to \texttt{g3}. The function for drawing the line uses \texttt{lm}, an abbreviation for Linear Model, which is used when drawing a straight line.
    \item[Line 2] What is \texttt{g3}? This instruction means to display it. You can also write \texttt{plot(g3)}.
\end{description}
