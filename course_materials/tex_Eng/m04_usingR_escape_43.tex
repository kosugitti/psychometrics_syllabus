\begin{description}
    \item[Line 1] We will now process the dat object. The result will be assigned to the dat.long object.
    \item[Line 2] We are extracting only the variables for fiscal year, name, and position from the dat object.
    \item[Line 3] We are creating a column of variables that have information about the row name or which row it is. The variable name is ID.
    \item[Line 4] We are converting it to a long type using the \verb|pivot_longer| function. At this time, the ID column is the key.
    \item[Line 5] We output the result.
\end{description}
