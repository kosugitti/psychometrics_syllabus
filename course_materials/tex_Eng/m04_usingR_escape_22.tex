\begin{description}
    \item[Line 1] The object named \texttt{g2} is created by adding a function that places points to the object named \texttt{g1}.
    \item[Line 2] What is \texttt{g2}? This is an instruction to display it. You can write \texttt{plot(g2)}.
\end{description}
