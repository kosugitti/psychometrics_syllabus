\begin{description}
   \item[Line 1] One of the data is assigned to the object \texttt{X} using the function \texttt{c} which means "combine".
   \item[Line 2] The other data is assigned to the object \texttt{Y}.
   \item[Line 3] t-test is performed with the arguments of the data \texttt{X, Y}, and the \texttt{paired} option is set to \texttt{T} which means 'paired'. The alternative hypothesis is set to "X is less than Y" as a \underline{one-sided test}.
\end{description}
