\begin{enumerate}
    \item Download the sample data \texttt{BaseballDecade.csv} and save it in the project folder.
    \item Execute \texttt{code:\ref{code:05_exe1}} to clean the data.
    \item Use the function \verb|summary| to summarize the entire data object and display its descriptive statistics.
    \item Calculate the mean value of the height variable. The function for finding the mean is \verb|mean|.
    \item Calculate the standard deviation of the weight variable. The function for finding the standard deviation is \verb|sd|.
    \item Calculate the median of the salary variable. The function for finding the median is \verb|median|.
    \item Calculate the correlation coefficient between height and weight. The function for finding the correlation coefficient is \verb|cor|, which takes two arguments.
\end{enumerate}
