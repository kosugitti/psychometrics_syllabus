\begin{lstlisting}[language=c++,label={code:usingR_pivot_wider},caption={Function to Convert to Wide Format}]
dat.long %>% 
    pivot_wider(id_cols = ID, names_from = name, values_from = value)
\end{lstlisting}
