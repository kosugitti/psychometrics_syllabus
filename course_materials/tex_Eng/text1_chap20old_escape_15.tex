Translation:

\begin{lstlisting}[language=c++,label={code:20_09},caption={t-test Part 2}]
X <- c(34, 25, 13, 21, 13, 26, 19, 12, 19, 19)  # X-values
Y <- c(35, 30, 16, 27, 20, 28, 16, 14, 18, 21)  # Y-values
dat <- data.frame(condition = c(rep(1, 10), rep(2, 10)), value = c(X, Y))
dat$condition <- factor(dat$condition, labels = c("pre", "post"))  # Two conditions: "pre" and "post"
t.test(value ~ condition, data = dat, paired = T, alternative = "less")  # Perform a paired t-test to compare the means of the conditions
\end{lstlisting}
