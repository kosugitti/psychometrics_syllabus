\begin{description}
   \item[Lines 1-3] Creating a data frame. ID refers to the subject number, which is a repeated sequence of 1 to 4 (3 times) within each group (\texttt{rep(1:4,3)}). The condition variable has three types, with each type comprising of four individuals (\texttt{rep(1:3,each=4)}), and the last dependent variable (value) must be manually entered.
   \item[Lines 5-7] Converting the condition variable into a factor. The labels are "control" for the control group and "exp1" and "exp2" for experimental group 1 and 2, respectively. 
\end{description}
