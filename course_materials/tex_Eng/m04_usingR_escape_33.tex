\begin{description}
    \item[1st line] The data is in the \texttt{dat} object, with height variable on the x-axis and weight variable on the y-axis. The plot will be colored based on teams.
    \item[2nd line] We will try to plot a scatter plot.
    \item[3rd line] We will try to draw a line that fits the data.
    \item[4th line] The data is in the \texttt{dat} object, with teams on the x-axis and salary variable on the y-axis. The plot will be filled with color based on teams.
    \item[5th line] We will try to draw a boxplot.
\end{description}
