\begin{description}
   \item[1st line] Put the regression equation and data into the \texttt{lm} function to perform analysis, and assign the result to the \texttt{result} object. By the way, the \verb|~| symbol is called a tilde\footnote{In the Japanese keyboard layout, it can be found on the key of "he" located right next to "ho" key, with Shift pressed. In the US keyboard layout, it can be found above the Tab key.}.
   \item[2nd line] Output a summary of the analysis result.
\end{description}
