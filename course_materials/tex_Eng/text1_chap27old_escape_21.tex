\begin{description}
   \item[Lines 1-5] Constructing data in a dataframe format. Note that the data is the same as before, but the way it is assembled has changed slightly. To verify, it's a good idea to display what \texttt{Example3} looks like.
   \item[Line 6] Setting nominal-scale explanatory variables as a factor type.
   \item[Line 8] Running a linear model. Note the presence of \texttt{brm} and the addition of the term \texttt{(1|ID)}.
   \item[Lines 9-10] Outputting the results. 
\end{description}
