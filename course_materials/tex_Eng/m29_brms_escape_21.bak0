\begin{description}
   \item[Lines 1-5] Assembling the data into a data frame format. Please note that the data is the same as last time, but the way it is put together is slightly different. For confirmation, it is a good idea to display \texttt{Example3} to see what it looks like.
   \item[Line 6] Setting the nominal scale level explanatory variables as factor type.
   \item[Line 8] Running a linear model. Please note that it is identified as \texttt{brm}, and that the term \texttt{(1|ID)} has been added.
   \item[Lines 9-10] Outputting the results. 
\end{description}
