# WORKLOG

## 2026-08-23〜25 KDP版(V3ベース)のラッパ作成と試しビルド

### BasicBook3_kdp.tex を新規作成

V3(ch系)でKDP体裁のPDFが組めるかを試した。旧KDP版 `v1_2/course_materials/tex/Dkiso1_book_kdp.tex`
の体裁を移植したもので，**本文は BasicBook3.tex と同じ ch*.tex を読む**。違いは5点だけ。

- `header_X1A.tex` を読んで **PDF/X-1a 出力**（GTS_PDFX・OutputIntent の埋め込みを確認済み）
- `\geometry{twoside,inner=32truemm,outer=20truemm}`（綴じ側を広く）
- 章扉のノンブルを消す（`\thispagestyle{empty}`）
- **行番号を出さない**（`\linenumbers` を除去。紙面には不要）
- ヘッダの版数表記を消し，**あとがき・奥付**を追加

巻末付録には**新シラバス(b系)30本**を `\chapter{本講義に対応する詳細シラバス}` の下に収録した。
旧KDP版は s系(v1_2)を収録していたが，V3ベースなのでb系に切り替えた。
b*.tex は `\section` 始まりで `\documentclass` を持たないため `\input` でそのまま取り込める。

**ビルド結果: 491ページ・エラー0・未定義参照0・未定義引用0・図の読込失敗0**
（シラバスなしなら437ページ。通常版431ページとの差はtwosideマージンぶん）

### ★ KDPビルドには `*_versions*.md` が必要

`masthead.tex`(奥付)が `../../Book_versions1.md` と `../../Syllabus_versions1.md` を
`\input` して版数を刷り込んでいる。これらは compile スクリプトが生成するファイルなので、
**Dropbox外でビルドする際はコピーを忘れるとビルドが止まる**（`File not found` で Emergency stop）。
今回2回踏んだ。リポジトリ直下の `*_versions*.md` は10件ある。

### あとがきの修正

- 「今はメジャーバージョン2です」→ **3** に修正。本文自身が「シラバスの骨組みが変わればメジャーが上がる」
  と書いており、V3で骨組みが変わって3になっているのに記述が追随していなかった

### 作業用PDFを配置

`psychometrics_syllabus/BasicBook3.pdf` に**431ページ版**（校正461件・ch28/29/30完成を反映）を配置。
これまで置かれていたのは8/18の427ページ版（校正前）だった。git追跡外の作業用ファイル。

**配布用の `kosugi-stats/pdfs/BasicBook3.pdf` は8/7版のまま**で、まだ更新していない。
版を3.30.0で確定してから更新する方針。

### 決まったこと・次にやること

**方針: 3.30.0 でKDP版と配布版を揃えてスタートする**（小杉先生の判断）。通し読みの校正が終わってから。

未着手の作業:
1. **バージョンを3.30.0に上げる**。ただし `compile1text.sh` は**パッチを+1する仕組みしか持たない**
   （`plusOne=expr $patch + 1`）ので、そのまま回すと3.29.6になる。マイナー/メジャーを上げる
   オプション（`--minor` / `--major`）をスクリプトに足すのが望ましい。手動で書き換える場合は
   `BasicBook3.tex` の4行目と `Book_versions1.md` の両方を直し、**compileスクリプトを通さずにビルド**する
2. **奥付の著作権表示**が `Copyright (C) 2021` のまま。`2021-2026` などに更新するか要判断
3. **あとがきを2本立てにする**。「初版にむけたあとがき」は残し、新しいものを追加する方針。
   本文は先生が別途執筆される
4. 前書き(`preface1.tex`)は版に依存しない内容なので現状維持でよいが、別途書かれる場合は組み込む

### 未完了・次回への引き継ぎ

- **原稿の通し読み校正が進行中**。手が入ったらKDP版・通常版とも再ビルドが要る
- KDPラッパ自体は完成しており、ビルドも通っている。残るのは上記1〜3の体裁まわりだけ


## 2026-08-20〜23 ch28/ch29/ch30の完成と全30章の最終校正（461件）+ bib統合

### ch29「ベイズを実践する」の完成

- brms節・Stan節を新規執筆。MCMC・確率的プログラミング言語・生成量は ch27.old から移設
- 題材は BaseballDecade.csv（2020年度・野手329名）に統一。**ch09で散布図 → ch24で最小二乗と最尤 → ch29でベイズ**という縦串が通る形にした
- lm() と brm() を並べ，点推定値がほぼ一致すること（切片 -124.92 vs -124.90）を見せたうえで，得られるものの性質の違い（1組の点推定 vs 4000組の乱数）を説明する構成
- Stan環境構築の補遺を追加（OS別の手順・cmdstanrは公式のr-universe経由に修正）
- 図は事後分布をパラメータごとにファセットで分けて描き直した（切片-125・傾き1.17・sigma7.46でスケールが違い，同一軸だと潰れるため）
- BayesRTMB・lme4・lmerTest の引用を CRAN の Citation で確認して myBiber.bib へ追加

### ch28「JASPでベイズ分析を体験する」の完成

- **本文が空（285字のスケルトン）だった**ため .old から移設。見出し4本が並ぶだけのページになっていた
- JASP 0.98.1 に合わせてスクリーンショット12枚を差し替え。旧版は0.95.4
- 起動画面の説明が実物と食い違っていた（「JASPは猛烈なスピードで開発されています！」という文言が現行画面に存在しない）ため書き換え
- t検定の出力が**片側検定で撮られており**，本文・表の両側検定の p 値（.029/.026）と食い違っていた。撮り直していただいて解決
- JASP_plot01/plot02/cor1 の3枚は対応するショットがなく0.95.4のまま。**内容に差がないため据え置きと判断**（決着済み）

### ch30「後期のまとめ」の完成

- .old から移設し，ch27.old の「ベイズの世界の広がり」を見出しを立てずに地の文として溶かし込み

### 全30章の最終校正（461件）

**フェーズ1 誤字脱字・誤変換 215件**。エージェント7体で全30章・約58万字を分担通読させ，指摘を1件ずつ検証して適用。実害が大きかったもの:
- ch05 分散の展開式に開き括弧のない `)` が5行分，第2項に余分な N
- ch06 クロス集計表の参照4箇所が無関係な表（datasaurus）を指していた
- ch12 **脚注のネストで内側の本文がPDFから消えていた**。「非心度」の説明が印字されず参照番号だけが浮いた状態。ビルドは通るため発見しにくい
- ch18 数式ラベルが `\[ \]` 内にあり3箇所の参照が誤番号を拾っていた → equation環境化
- ch22 コードラベルの取り違えで本文の参照が200行離れた別コードに飛んでいた
- ch24 **対数尤度の式が丸ごと欠落**（「次のようになります」の直後が空行）→ 補完

**フェーズ2 表記ゆれ 168件**。線型→線形，条件付き→条件つき，従って→したがって，時に→ときに など。「様に」「所」は複合語（同様に・場所・所持金）を正規表現で除外して誤変換を防いだ。実際に変換が必要だったのは「時に」だけで，「所」は14件すべてが「所持金」だった

**フェーズ3 論理・内容 32件**。
- ch01 落体の説を聖書→アリストテレス，マクスウェル→ラプラスのデモン，ルネサンス→科学革命
- ch02 大型計算機のOSがLinuxになっていた（同章内でUnixとも書いており矛盾），1byte=128→256種，1000倍→1024倍
- ch05 平均所得以下の割合 59.6%→62.2%（厚労省の原典PDFを取得して確認）
- ch13 χ²値 9.9627→23.095（chisq.testで検算。ch06の値を流用していた）
- ch16 対立仮説の向きが英文と逆（和文「0より小さい」vs 出力 greater than 0）
- ch22 `rep(...,10)` と長さ5の列で**データが2重に複製される**。Rはリサイクルでエラーを出さないため気づけない
- ch27 7/24→7/21（n=24 では p=0.041 となり「5%より生じやすい」という本文の結論が崩れる）

**フェーズ4 索引の粒度 46件**。読みの割れ4件（t検定・F検定・MCMC法・ベイズ法。ch30だけ流儀が違い索引が2箇所に割れていた）を解消し，主要6系列を親子登録に変換。尺度水準の下に名義・順序・間隔・比率が並び，第4講の概念が一望できるようになった

### bibの統合

**方針: 同じものの複製を置かない（`~/Dropbox/myBiber.bib` 一本）**

- symlink案は採らなかった。compile系スクリプトが毎回 `cp ../../../myBiber.bib ../../syllabus_bib.bib` を実行しており，**symlinkにしても次のビルドで実ファイルに戻される**ため
- `myStyle.sty` の参照先を `syllabus_bib.bib` → `../../../myBiber.bib` に直接変更
- compile系スクリプト11本から上記の `cp` 行を削除
- 複製の実体だった `syllabus_bib.bib` を削除
- **sbib.bib は残した**。v1_2（旧KDP版）の Dkiso2_book.tex 等が `\bibliography{../../sbib}` で実際に参照しているため。現行テキストは sbib を引いていない

### 中間ファイルの整理

- `.gitignore` に .log/.ind/.idx/.ilg/.bbl/.blg/.bcf/.run.xml/.dvi/.fls/.fdb_latexmk/.synctex.gz/.lot/.lof/.ltjruby を追加（SocialPsychology/tex/logs/*.log は整形記録なので除外指定で残す）
- 追跡されていた中間ファイル42件を追跡対象から外し，作業ツリーの264件を削除
- **compile系スクリプト9本の掃除リストを拡充**。取りこぼしていた .bcf/.run.xml/.lot/.lof と章別コンパイルの副産物（ch*.log, ch*.pdf）もビルドの最後に消えるようにした

### 検証とコミット

- BasicBook3 フルビルド: **429ページ・エラー0・未定義参照0・未定義引用0・biber警告0・upmendex警告0**
- **Dropbox配下ではビルドが静かに中断する**（130ページ目で止まりPDF末尾がnullバイトになる。エラーメッセージは出ない）ため，検証はすべて Dropbox 外にコピーして実行した
- commit a2c162b1（校正461件・81ファイル）／ commit 68297b74（bib統合・中間ファイル整理）でpush済み

### 未完了・次回への引き継ぎ

- ch28 の JASP スクショ3枚（plot01/plot02/cor1）は0.95.4のまま。内容に差がないため据え置きと判断済み
- **KDP版のV3ベース出し直し**に着手可能になった。ブロッカーだった ch29 が完成したため。残るのは KDP用体裁（奥付masthead・あとがき・twoside内外マージン・表紙）を `BasicBook3_kdp.tex` ラッパとして移植する機械作業と，巻末付録の旧シラバス s系→新 b系への差替え判断
- ch28 の JASP は最新版に追随したが，**JASPは開発が速い**（0.16→0.9台→0.98.1）。次の改訂時にまたスクショの確認が要る
- 心理統計演習（m系）の SEM 章は未着手のまま


## 2026-08-07 ベイズ章群(ch25-27)の執筆・用語の全数点検・索引整備

### ch25「3つめの推定法」/ ch26「分布で推定する」
- 本文を執筆。ch25は「3つの推定方法の比較」をch26から受け取り，モンティ・ホール問題を章末補遺へ。
- **ch25とch26で「事前分布と尤度の折衷」節が重複していた**。もともとch26の担当(ch26.oldにのみ存在)で，
  ch25執筆時に紛れ込んだもの。ch26に残してch25から削除。`\label{fig:26_10}` と
  `\keyterm{無情報事前分布}` の二重定義も同時に解消。シラバスもb26だけに戻した。
- 課題を新設。ch25は文章題4問(同時確率・周辺確率・条件つき確率／有病率0.5%と10%での陽性的中率の対比)，
  ch26は旧版のノリに合わせて2問。

### 専門用語の英語表記を全数点検
`\keyterm` 1,619箇所・616種を突き合わせ，**275種を修正**。
- 用語自体の誤り: 四分位 quantile -> **quartile**（同じch05の別行では Inter-Quartile Range と正しい），
  確率分布 random distribution -> probability distribution，正規化定数 normalized -> normalizing，
  確率的プログラミング言語 stochastic -> probabilistic，弁別的妥当性 -> discriminant validity
- ラテン語句: Maximum A Posterior -> maximum a posteriori 等（表記規約のEAP/MAPに準拠）
- 綴り: Principle -> **Principal** Component Analysis，Marcov->Markov，Greaded->Graded，
  Infromation->Information，Varidity->Validity，Singular balue->value ほか30種以上
- 大文字小文字を小文字に統一。固有名詞・略号・統計記号(F-test/R-squared/Q-Q)は保持
- confidence interval の綴り(confidential/confidencial)6箇所
- **架空の引用キー `aoki2021` を発見・削除**。bibに一度も存在せず，2025-11-12から9か月弱
  未定義のまま通っていた。全リポジトリを洗ったが架空文献はこれ1件のみ。
  ほかに実在するのにキー名が違うものが3件(Wasserstein2016 / Lambert201805 / Jerry2019)。

### 索引
- 読みが英語で節を間違えていた2件を修正(ニット knit->にっと，メタ分析 meta->めた)
- 8組を親子(subitem)化。`\keytermsub` / `\keytermLsub` をmyStyleに追加
- **R・操作用語索引(ridx)を新設**。`\keytermR` / `\keytermRL` で17語を分離
- SocialPsychologyは索引が空だと誤認したが，**実際は `\index[nameidx]` で555箇所・
  `\index[termidx]` で886箇所が整備済み**だった(`\index{` で検索したため見落とした)。
  真の原因は **compileSP.sh に upmendex が無い**ことで，PDFに反映されていなかった。2行追加して解決。

### ch27
先生が本文を執筆。校正で16箇所を修正。**コイン投げが24回と書かれていたが，脚注の
`chisq.test(c(7,14))`・X2=2.33・p=0.1266 は21回のときの値**で，24回だと p=0.0412 で
有意になり本文の主張が崩れる。21回に統一した。b27も細目名・BF記法($BF_{12}$)・
解釈基準の記述を本文に合わせた。

### compile系スクリプト
- **`--push` を付けたときだけ commit/push する形に変更**(10本)。既定はコンパイルのみ
- 前回入れた `set -e` がスクリプトを壊していた。myStyle.sty の
  `\newtcbtheorem[number within=chapter]{Rscreen}` が chapter カウンタを持たない
  ltjsarticle(シラバス)でエラーを出し，lualatexが終了コード1を返すため最初のパスで中断していた。
  `\@ifundefined{c@chapter}` で分岐させて根治。あわせて全スクリプトの lualatex に
  `-interaction=nonstopmode` を付与(従来は対話プロンプトで停止していた)
- 表紙PDF `Scaling/images/cover3.pdf` と `SocialPsychology/images/coverSP.pdf` を作成。
  参照されていたが存在せず pdfpages エラーが各9件出ていた。`.gitignore` の `*.pdf` に
  食われて一度も追跡されていなかったので，表紙だけ例外を追加

### 未了・次回への引き継ぎ
- **BasicBook3.pdf の再生成が残っている**。ソースの修正は全て済み・push済みだが，
  Dropbox配下でlualatexが `Input/output error` で落ちるためPDFを作れていない(下記)
- **前回kosugi-statsへ配布したBasicBook3.pdfは索引が壊れている**。imakeidx が makeindex を
  自動実行して upmendex の結果を上書きし，五十音の見出しが消えて文字コード順になっていた。
  `noautomatic` で修正済みだが，PDFの差し替えが必要
- ch28(JASP更新)・ch29(brms/Stanの2節が新規執筆)が残り。ch29が最優先ブロッカー

### Dropbox配下でlualatexが落ちる問題（重要）
リポジトリの実体は `~/Library/CloudStorage/Dropbox/` でDropboxのファイルプロバイダ配下。
BasicBook3のビルドが `lualatex: Input/output error` で不定の位置(173/192/224ページ等)で落ちる。
- ディスクを19GB->157GBに空けても解消せず(容量は原因ではない)
- **同じソースをDropbox外(/private/tmp)にコピーすると395ページ・エラーゼロで通る**
- 図版PNGを数百読む処理でファイルの実体化が間に合わないためと思われる。40MBのPDFを
  繰り返し書いて同期を走らせ続けたのが引き金
- 対処案: 再起動でファイルプロバイダを再初期化して様子を見る。駄目なら
  コンパイルスクリプトを「Dropbox外にコピーしてビルドしPDFだけ書き戻す」形に変える

### ディスクの棚卸し（副産物）
「システムデータ483GB」の正体を特定。シミュレーション出力は3GB弱で無実だった。
- `~/Library/Caches/Homebrew/downloads` 50GB -> `brew cleanup --prune=all` で90MBに
- `~/Library/Caches/Wolfram/WolframScript/WolframScriptTemporary` 54GB(7/26の一時ファイル) -> 削除
- ゴミ箱32GB -> 空にした
- `/System/Volumes/VM` のスワップ70個70GB(7/22-26付) -> 再起動で回収予定
空き 19.3GB -> 157GB

## 2026-07-08 現行KDP版(m系)とV3(BasicBook3/ch系)の差分棚卸し + KDP出し直し方針

### 背景
Amazon KDPで出している『心理学データ解析基礎』の誤字脱字が多いので，V3(BasicBook3)ベースで作り直したいという相談。現状を突き合わせた。

### 二重構造の確認
- 現行KDPの本 = `Psychometrics/v1_2/course_materials/tex/Dkiso1_book_kdp.tex` → 旧 m系(`m01`〜`m30`，同ディレクトリ)。誤字が残る古い版。
- V3(育てている方) = `contents_basic/BasicBook3.tex` → ch系(`ch01`〜`ch30`，v3.29.3・410ページ)。ここ数か月の修正(ch07/ch08回帰例の全面改訂・ベイズ章群b25-29再編・ch10色/誤字)は全てこちら。KDPソース(m系)には未反映。
- ビルドスクリプト `compile1KDP.sh` のパス `course_materials/tex` はリポジトリ直下を指すが実体は空。真のKDPソースは `Psychometrics/v1_2/course_materials/tex/`（パスがずれている）。

### 章対応の要点（誤字レベルでなくコース全体の再編）
- 1A: 背骨を「記述→相関→回帰→確率→推論」に並べ替え。推論(旧m16/m17)を前期へ引き上げ，実験計画法(旧m12-14)を後期へ移動。測定の基礎(古典的テスト理論 m06)とR利用独立章(m04)を廃止，電子計算機の章(ch02)を新設。
- 1B: 分散分析のR実践をBetween(ch20)/Within(ch22)の2章に分割。ch24に一般化線形モデル追加。ベイズ章を改題・再編。
- 要注意: 旧KDPの m29「Rによるベイズ推定」は本文完備だが，V3の ch29 は節見出し4つだけの白紙(`ch29_bayesian_modeling.tex` 434バイト)。V3にそのまま切替えると最終章が後退する。

### シラバス対応
- 新シラバス b系(`syllabus_basic/b01`〜`b30`)は ch系と番号・ファイル名・トピックで1対1に対応済(2026-05-27時点で全章対応)。
- 残ズレ2点: (1)ch27章タイトルが本文「ベイズ推定と判断」／シラバスb27「ベイズ的判断の方法」で不一致(確定は後者，本文が古い)。(2)ch27本文の節が旧構成のままでシラバス4細目に未同期。→ ch27圧縮・再編タスクと同一。
- 現行KDP(Dkiso1_book_kdp.tex)は巻末に旧シラバス s系を付録綴じ込み。V3化時に新b系へ差替え/削除の判断が要る。

### 方針（夏休みの宿題）
V3ベースでKDPを出し直す方向で確定。出す前に埋める穴は優先順に:
1. ch29 本文の新規執筆（最優先ブロッカー，現状白紙）
2. ch27 の本文再編(18.5k→10k字)＋章タイトルのシラバス同期
3. ch28 の JASP 最新バージョン対応（スクショ・メニュー名）
加えてKDP用の体裁移植(奥付masthead・あとがき・twoside内外マージン・表紙)を `BasicBook3_kdp.tex` ラッパとして用意する機械作業が残る。今日は着手せず記録のみ。

## 2026-06-19 ch08 図8.5〜8.8の仕上げ調整 + フルビルド・サイト反映

### 図の調整(先生のレビューを反映)
- 図8.5(部分相関)・8.6(偏相関): 矢印を曲線→直線に。相関なので双方向(`<->`)。8.7(回帰する)は直線だが片方向(`->`)を維持(回帰は方向あり)。
- 図8.6/8.7: テスト点を勉強時間と単語数の水平中央に配置し，単語数を左へ寄せ，勉強時間と同じ高さ(y中心0)・同じ太さ(0.8cm)に統一。勉強時間→単語数の矢印が水平になり，左右対称の三角形レイアウトに。座標は本文TikZを直接書き換え(`ch08_MultipleRegression.tex`)。
- 図8.8(偏回帰係数の3D, scatterplot3d): 旧版は単語数固定で勉強時間+1の赤L字。上昇b1=1.26がy軸40-100に対して小さく「移動」が読めなかった → 勉強時間を大きめ(9→18)に動かす赤矢印「勉強時間を動かす(単語数は一定)」＋青矢印「テスト点の上昇」＋点線斜辺「傾き=b1」に作り替え(`Rcode/figs_ch08_3d.R`)。

### 検証・ビルド・反映
- TikZは lualatex 単体コンパイル＋standaloneプレビュー画像で逐次確認(先生に画像送付して承認)。
- フルビルド `compile.sh 1` を計3回(v3.29.1→3.29.3)。毎回410ページ・致命的エラー0(残はOverfull警告のみ)。
- **掃除の注意**: `compile.sh` の cleanup_tex は .aux/.toc/.bbl 等は消すが `.log/.pdf/.old` は消さない。contents_basic に BasicBook3.{log,pdf,old}・error.log・古い章.log が残るので手動 `rm`。
- **サイト反映は手動**: `compile.sh` はサイトに触れない。ROOT の BasicBook3.pdf を kosugi-stats の `pdfs/` と `docs/pdfs/` 両方にコピー → commit/push(GitHub Pages, SSH remote)。
- 最終コミット: psychometrics_syllabus `10c07123`，kosugi-stats `8deca4a`(v3.29.3)。


## 2026-06-17 ch07/ch08 回帰例を「勉強時間・単語数→テスト点」に全面改訂 + ch10色修正

### 背景
- ch08のGPA例が値5.34〜8.40でGPA上限4を超える誤り，入試1000点満点も非現実的。先生判断で案3(全面改訂)を採用。新題材「1週間の勉強時間・覚えた英単語数→期末テスト点」(身近・説明変数の単位差で標準化を見せる・説明変数同士の相関で偏相関を語れる)。

### 確定データ(15人, Rcode/figs_ch0708.R に格納)
- 勉強時間6-25時間/週，単語数150-500語/週，テスト点47-92。
- 単回帰(ch07): テスト=38.92+1.996×勉強，R²=0.820。重回帰(ch08): テスト=33.43+1.258×勉強+0.0511×単語，R²=0.887。
- 相関 勉強-単語0.792/勉強-テスト0.906/単語-テスト0.875，標準化係数 勉強0.571/単語0.423，偏相関(単語-テスト|勉強)0.609。
- 単位差: 生係数25倍差→標準化で比較可能。単回帰傾き1.996→重回帰1.258で多重共線性も語れる。

### 本文・表
- ch07: tbl:07_01差し替え+本文22箇所(変数名・係数・予測例・R²・課題)。Python anchored replace(表)+一意文字列replace(本文)で件数検証しながら実施。
- ch08: tbl:08_01/02/03差し替え+本文24箇所。サークル/バイト例・家賃課題は別例として維持。
- 残存旧ラベル(内申/入試/GPA/評定)ゼロをgrep確認。

### 図(ggplot2+ragg+theme_bw / scatterplot3d / TikZ)
- データ図6枚 ggplot2+ragg+Hiragino Sans+theme_bw: ch07 simple_scatter/linerModel/yhatg/bestFit，ch08 corrMat2/corrMat3(GGally::ggpairs)。raggで日本語文字化けなし。
- 3D 2枚 scatterplot3d: surface(回帰平面)，partial_slope(偏回帰係数の傾きを赤L字)。par(family=Hiragino Sans)で日本語軸ラベル。
- 概念図 partial1-4 を TikZ で本文に直書き(\includegraphics廃止)。テスト点/勉強時間/単語数の新ラベル，部分相関/偏相関/回帰する の矢印。partial1-4.png は未参照化(削除可)。corrMat.png(家賃)は変更なし。
- lualatexで ch08 単体12ページ エラーなしコンパイル確認。

### ch10
- 図10_18の色記述を修正: 標準正規分布=緑，μ=1=赤，μ=-2σ=0.5=黒。先生が黒→緑修正，誤字「そのなの→その名の」を当方修正。

### 引き継ぎ
- 図の再現スクリプト: Rcode/figs_ch0708.R(2D), Rcode/figs_ch08_3d.R(3D)。データもこの中。
- 最終は BasicBook3 フルビルドで相互参照を確認すること。


## 2026-05-27 ベイズ章群（b25-b29, ch25-ch29）のシラバス再編

### 背景

ch29「ベイジアンモデリングの世界」がスケルトン（4 section 見出しのみ）のまま放置されていることが発覚。あわせて ch27 が他章（7-9k字）の倍以上（18.5k字）に膨張しており，4 章全体のバランスを見直すことに。さらに ch25 のコマ細目「二次元の確率変数」と本文 section「ベイズの定理」が一致しないなど，シラバスと本文の対応にもズレが見つかった。

### 再編方針（確定）

ベイズ教育の縦串を以下で整理：

- b25: ベイズの定理を導入（数式と直観）
- b26: 推定結果＝分布という考え方の転換
- b27: 分布をどう判断に翻訳するか（NHST/ROPE/BF/PPC の 4 方法）
- b28: ツール（JASP）で実装に落とす
- b29: JASP の枠を越えてモデルを自分で組む世界の入口（Stan/brms）

各種モデリング（分散検定，打ち切り，混合分布など）は基礎コースのテキスト範囲から外し，3 年次「ベイズ統計学」（半期）に委ねる方針確定。経験上 ch29-30 は授業時間内に収まらないため，テキストとしての位置づけも「読み物・入口」に絞る。

### 各章の改訂内容（タイトルとコマ細目）

- ch25「もうひとつの推定: ベイズの定理」
  - コマ細目 1「二次元の確率変数」→「ベイズの定理の導出」（同時/周辺/条件つき確率 → 独立性 → 式変形によるベイズの定理導出を 1 細目に統合，本文構造に合わせる）
  - コマ細目 2「ベイズの定理の意味」据え置き
  - コマ細目 3「ベイズの定理を使った推論の例」据え置き

- ch26「分布で推定する」
  - 旧 3 細目 → 新 4 細目
  - 1. 事前分布と事後分布
  - 2. 事後分布を代表する数字
  - 3. 3 つの推定方法の比較（新設，本文 \section{3つの推定法} に対応・リネーム）
  - 4. 事前分布と尤度の折衷（旧「事前分布の影響」をリネーム，本文 section に合わせる）

- ch27「ベイズ的判断の方法」
  - 旧 5 細目 → 新 4 細目
  - 削除: 「ベイズを実践する方法」（MCMC・JASP 登場の歴史的話を b29 に移譲）
  - 1. ベイズ法と帰無仮説検定
  - 2. 確信区間と ROPE による判断（ROPE 派の主軸）
  - 3. ベイズファクターによる判断（実践は次コマに譲る橋渡し追加）
  - 4. 事後予測分布による判断（添え物，詳細は最終コマへ）
  - BF と並ぶ判断方法として PPC を b29 に押し出すか議論したが，b27 で「見取り図」だけ示し b29 で「実装」を扱う 2 段構成に決定

- ch28「JASPでベイズ分析を体験する」
  - タイトル変更のみ，4 細目据え置き（JASP の導入と IF / 記述統計と可視化 / 相関・回帰分析 / BF を使った検定）
  - 句読点規約「、」→「，」に統一
  - 「ベイズファクターをつかった検定」→「ベイズファクターを使った検定」（漢字化）

- ch29「ベイズモデリングへの入り口: Stanとそのフロントエンド」
  - 旧スケルトン 4 節（ベイジアンモデリングの基礎 / 分散のモデリングと検定 / 尤度を活かしたモデリング / 分布を混ぜ合わせるモデリング）を破棄
  - 新 4 細目
  - 1. 確率的プログラミング言語とは（PPL/Stan，MCMC の直観，b25-b28 で宙に浮かせた「裏で動く計算」の回収）
  - 2. brms による実用的モデリング（R 式記法 → Stan コード自動生成，make\_stancode()）
  - 3. 生成量と事後予測分布（b27 で添え物だった PPC を実装視点で拾い直す）
  - 4. 自分で Stan コードを書くために（brms 出力の Stan コードを読む，3 年次への橋渡し，BayesRTMB は脚注で「学習コストの観点から本書では Stan 直接を勧める」と注釈）

### 全章共通の修正

- 「科目の中でのこのコマの位置づけ」tabular を全章で除外（b04 のコメントアウト含む，b23/b25/b26 で実削除）
- s25_bayesian.tex / s26_bayesian2.tex（v1-v2 旧版）を削除

### typo 修正

- b25 コマ細目 3「ただし頻度主義的な確立の理解」→「確率」
- b26 コマ細目 1「分布関数出会っても」→「であっても」，「わかららない」→「わからない」，「ものであることとして捉え」→「ものとして捉え」

### 本文（ch 系）側の修正

- ch25 \chapter リネーム（「ベイズ推定の基礎」→「もうひとつの推定: ベイズの定理」）
- ch26 \chapter リネーム（「ベイズ推定の実際」→「分布で推定する」），\section「3つの推定法」→「3つの推定方法の比較」
- ch28 \chapter リネーム（「JASPによるベイズ推定の実践」→「JASPでベイズ分析を体験する」）
- ch29 スケルトンを新章タイトル＋新 4 セクションに置き換え（中身は空のまま）

### 確定事項（タイトル/方針）

- BayesRTMB は無用の長物との評価（RTMB 書法を学んだあと cmdstan 書法を再学習するコストが無駄）。脚注で言及のみ。
- 「GUI でモデルを描いたら cmdstanr コードが出るソフトはないか」→ ほぼ空席。brms `make_stancode()` が現状の最良妥協点。
- ch27 で PPC が添え物になるのは仕方ない（ROPE 派の姿勢に整合）。

### 残作業（夏休みに集中執筆予定）

本文側の書き直し・新規執筆が 4 章分残る：

1. ch27 本文の再編
   - 旧 \section{ベイズを実践する方法}（MCMC・JASP 登場の歴史節）を削除
   - 旧 \section{生成量と事後予測分布を使った判断} を「事後予測分布による判断」に縮小（添え物化）
   - 旧 \section{ベイズファクター} 末尾に「実践は次コマの JASP に譲る」を追記
   - 全体で 18.5k字 → 10k字程度に圧縮

2. ch28 本文の見直し
   - JASP の最新バージョンに合わせたスクショ・メニュー名の確認（必要なら更新）
   - 内容は据え置きで微調整のみ

3. ch29 本文の新規執筆
   - 4 セクション分を埋める
   - 線形回帰のベイズ版を brms で実装する具体例
   - posterior\_predict() / 生成量の例
   - make\_stancode() で brms が吐く Stan コードを読む例
   - 3 年次「ベイズ統計学」への橋渡し

4. 心理統計演習（m 系）の SEM 章新規執筆
   - 既知の TODO（別系統，今回の作業範囲外だが書かなければならないリストには載せておく）

執筆ペース方針: まとまった時間が取れる夏休みを目処としつつ，普段のセッションでも空き時間に「ちょっと書く？」と水向けして 1 節ずつ進める。

### コンパイルと git push

- `compile1syllabus.sh` でシラバスをビルド → バージョン 3.0.17 にアップ，PDF を `syllabus_basic.pdf` に出力
- commit `ab5b3f9e`: 27 files changed (4708/2248)。s25/s26 削除＋本セッションの全変更を含む
- スクリプト末尾の `git add --all` で `Psychometrics/GoogleFormMaker/google-forms-mcp/`（matteoantoci/google-forms-mcp の外部 clone）が embedded git repo（mode 160000 commit pointer）として誤って index 入りした。`git clone` した側ではフォルダ空＝中身が取れない状態

### embedded git repo の整理（追加対応）

- 選択 A（.gitignore 追加 + index から外す）を採用
- `.gitignore` に `Psychometrics/GoogleFormMaker/google-forms-mcp/` 追加
- `git rm --cached Psychometrics/GoogleFormMaker/google-forms-mcp` で index から削除
- commit `6ffb9d3c` で push 完了。今後 `git add --all` しても google-forms-mcp は記録されない
- ローカルの実体（Newton 上のフォルダ）はそのまま残置。他マシンで MCP が必要になったらそのマシンで個別 clone する運用
